# 自畵像 — 개발 계획서 (v3)

> 현재 날짜: 2026-04-14  
> 현재 버전: v2 (캘린더 홈, 나의 기록, 상담 기록 분리)  
> 목표 버전: v3 (Claude 스타일 채팅 우선 UX)

---

## 변경 전/후 요약

| 항목 | 현재 (v2) | 목표 (v3) |
|------|-----------|-----------|
| 홈 화면 | 캘린더 | "오늘" 대시보드 |
| 메인 인터페이스 | 탭(축어록/보고서/대화) | 대화창 중심 |
| 왼쪽 사이드바 | 학생/주제 목록 | 나의 기록 / 상담 기록 / 캘린더 (3-버튼 네비) |
| 오른쪽 사이드바 | AI 패널 | AI 보고서 만들기 / 슈퍼비전하기 |
| 기록 시작 | 목록에서 선택 후 탭 전환 | 대화창 + 버튼 클릭 → 선택 → 즉시 대화 |
| AI 역할 설정 | 주제 생성 시 입력 | 프리셋 선택 + 직접 입력 |
| 대화 → 일기 변환 | 없음 | 블록 선택 후 AI 변환 |

---

## Phase 1 — 레이아웃 재구성

**목표**: 3패널 레이아웃 전환 (왼쪽 네비 사이드바 / 중앙 대화창 / 오른쪽 AI 사이드바)

### 1-1. 왼쪽 사이드바 단순화

```
┌──────────────┐
│  自畵像       │
│              │
│  ● 나의 기록 │  ← 클릭: 중앙에 나의 기록 대화창 표시
│  ● 상담 기록 │  ← 클릭: 중앙에 상담 기록 대화창 표시
│  ● 캘린더    │  ← 클릭: 중앙에 캘린더 표시
│              │
│  최근 기록   │  ← 최근 5개 빠른 접근
└──────────────┘
```

- `js/render-sidebar.js`: 3버튼 네비 + 최근 기록 리스트로 교체
- 기존 학생/주제 목록 UI → 대화 흐름으로 이동 (Phase 2)
- 색상: 나의 기록(green `#1D9E75`), 상담 기록(teal `#0F6E56`), 캘린더(neutral)

### 1-2. 중앙 대화창 (새 컴포넌트)

```
┌────────────────────────────────────────┐
│  [오늘] 2026년 4월 14일 월요일         │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │  [나의 기록 — 일기] 1번째 기록   │ │
│  │  오늘 상담에서 ~~~ (본문 미리보기)│ │
│  └──────────────────────────────────┘ │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │  [상담 기록 — 별-01] 3회기       │ │
│  │  2026-04-14                      │ │
│  └──────────────────────────────────┘ │
│                                        │
│  ────────────────────────────────────  │
│  [+ 나의 기록 시작]  [+ 상담 기록 시작]│
└────────────────────────────────────────┘
```

- 홈 화면 역할: 오늘 날짜 + 오늘 기록 요약 카드
- 하단 CTA 버튼 2개 → 대화 시작 흐름 진입
- `js/render-home.js` 새로 생성

### 1-3. 오른쪽 AI 사이드바 재구성

```
┌─────────────────────┐
│  AI 도구             │
│                     │
│  [보고서 만들기 ↗]  │  ← 현재 선택된 기록 기반
│  [슈퍼비전 시작 ↗]  │  ← 현재 선택된 회기 기반
│                     │
│  최근 보고서 요약   │
│  ···                │
└─────────────────────┘
```

- `js/render-aipanel.js`: 2버튼 중심으로 단순화
- 선택된 컨텍스트(기록/회기)에 따라 버튼 활성/비활성 자동 전환

**변경 파일**: `js/render-sidebar.js`, `js/render-aipanel.js`, `js/render-main.js`, `index.html`, `style.css`

---

## Phase 2 — 대화 시작 흐름 (채팅 우선 UX)

**목표**: 버튼 클릭 → 선택 → 즉시 대화 시작

### 2-1. 나의 기록 흐름

```
[+ 나의 기록 시작]
         ↓
  ① 주제 선택 모달
  ┌─────────────────────────────────────┐
  │  어떤 주제로 기록할까요?            │
  │                                     │
  │  ● 일기          (감정 친구)        │  ← 기존 주제
  │  ● 임용 공부     (학습 코치)        │
  │  ● 아쉬운 점     (성찰 코치)        │
  │                                     │
  │  [+ 새 주제 만들기]                 │
  └─────────────────────────────────────┘
         ↓ (주제 선택 시)
  ② 대화 시작 (AI 첫 메시지 자동 전송)
  → 중앙 대화창에 실시간 채팅 UI 표시
```

**새 주제 만들기** 시:
```
주제 이름: [                    ]
AI 역할:
  ○ 전문 코치        — "문제를 구조화하고 솔루션을 제시하는 코치"
  ○ 감정 상담사      — "감정을 들어주고 공감하며 함께 이해하는 친구"
  ○ 인생 설계사      — "장기 목표를 중심으로 현재를 정렬하는 멘토"
  ○ 그냥 들어주기    — "판단 없이 경청하고 필요할 때만 반응"
  ○ 직접 설명        — [역할을 자유롭게 입력...                  ]
```

### 2-2. 상담 기록 흐름

```
[+ 상담 기록 시작]
         ↓
  ① 내담자 선택 모달
  ┌─────────────────────────────────────┐
  │  어느 내담자의 기록인가요?          │
  │                                     │
  │  ● 별-01 (중2) — 3회기              │
  │  ● 달-02 (중1) — 1회기              │
  │                                     │
  │  [+ 새 내담자 등록]                 │
  └─────────────────────────────────────┘
         ↓ (내담자 선택 시)
  ② 축어록 입력 (인라인)
  ┌─────────────────────────────────────┐
  │  날짜: [2026-04-14]                 │
  │  축어록: [                          │
  │           ...                       │
  │          ]                          │
  │  [저장하고 슈퍼비전 시작]           │
  └─────────────────────────────────────┘
```

**구현**:
- 모달 컴포넌트 (`js/modal.js` 신규) — 주제 선택, 내담자 선택에 공용
- Phase 1 사이드바 변경과 연동

---

## Phase 3 — AI 역할 프리셋 시스템 ✅ 확정

**목표**: 주제 생성 시 UI에서 프리셋 선택 가능, DB에 `aiPrompt` 자동 저장

### 프리셋 정의 (4종 확정)

```js
const AI_ROLE_PRESETS = [
  {
    id: 'coach',
    label: '코치',
    prompt: '목표를 명확히 하고, 현재 상황을 구조화하며, 실행 가능한 다음 단계를 함께 찾는 코치처럼. 질문으로 상대가 스스로 답을 발견하도록 안내한다.',
  },
  {
    id: 'counselor',
    label: '상담사',
    prompt: '감정을 충분히 듣고 공감하며, 판단하지 않고, 상대가 자신의 감정을 이해하도록 돕는 상담사처럼. 해결보다 이해가 먼저다.',
  },
  {
    id: 'advisor',
    label: '조언가',
    prompt: '상대의 상황을 파악한 뒤 경험과 지식을 바탕으로 솔직하고 구체적인 의견을 주는 조언가처럼. 듣는 것보다 말하는 것을 두려워하지 않는다.',
  },
  {
    id: 'custom',
    label: '직접 입력',
    prompt: '',  // 사용자 입력
  },
];
```

**구현**:
- `js/state.js`: `AI_ROLE_PRESETS` 상수 추가
- `js/render-forms.js`: 새 주제 폼에 라디오 버튼 + 커스텀 입력 UI 추가
- `js/crud.js`: `saveTopic()`에서 프리셋 선택 처리

---

## Phase 4 — AI 텍스트 변환 공통 레이어 ✅ 확정

**목표**: 날것의 텍스트/대화를 AI가 정돈된 형태로 변환. 두 기능이 같은 서버 엔드포인트 + 패턴 공유.

### 축어록 AI 정리 (상담 기록)

- **범위**: 포맷 정돈만. 내용 손실 없음 (임상 근거 자료 훼손 방지)
- **정돈 내용**: 발화자 표기 통일(`상담자:` / `내담자:`), 비언어 주석 형식 정리(`[침묵 N초]`), 줄바꿈/공백 정리
- **위치**: 오른쪽 AI 패널 버튼 `[축어록 AI 정리 ↗]`
- **결과**: 정돈된 텍스트로 축어록 필드 업데이트 (원본 백업 유지)

### 일기로 변환 (나의 기록)

- **범위**: 대화 블록 → AI 재구성. 형식 변환 (대화체 → 1인칭 서술)
- **흐름**: 대화 메시지에 체크박스 → 1개 이상 선택 → 오른쪽 패널 `[일기로 변환 ↗]` 활성

### UX 흐름 (일기 변환)

```
대화창 (AI ↔ 사용자 메시지들)

각 메시지 옆: [☐] 체크박스 (hover 시 표시)
                        ↓ 1개 이상 선택 시
                  [선택한 내용으로 일기 만들기] 버튼 활성
                        ↓ 클릭
              AI가 선택 블록 기반으로 일기 초안 생성
              ┌────────────────────────────────────┐
              │  [일기 초안]                       │
              │  오늘은 상담 중에 ~~~ 느꼈다.      │
              │  ~~~~ (AI 작성)                    │
              │  [편집]  [저장]  [취소]            │
              └────────────────────────────────────┘
                        ↓ [저장]
              나의 기록 → '일기' 주제에 자동 저장
              (또는 선택 주제에 저장)
```

### 기술 구현

**공통 API 엔드포인트** (`server.py`):
```python
POST /api/transform-text
body: {
  mode: 'verbatim' | 'diary',
  # verbatim: 축어록 포맷 정돈
  text: string,
  # diary: 대화 → 일기 변환
  blocks: [{ role, text }],
  date: string,
}
→ { result: string }
```

**프론트엔드**:
- `js/transform-text.js` 신규: 두 모드 통합 (블록 선택 상태 관리 + 변환 API 호출)
- 대화 메시지 렌더링에 체크박스 추가 (`render-session.js`, `render-myrecords-view.js`)

### AI 프롬프트

```
아래 대화 중 선택된 부분을 기반으로 개인 일기 초안을 작성해주세요.

[선택된 대화 블록]
사용자: ~~~
AI: ~~~
...

규칙:
- 1인칭 과거형으로 작성
- 대화에서 등장한 감정, 사건, 생각을 중심으로
- 200-400자 분량
- AI 조언은 제외, 사용자 경험 위주
- 마크다운 사용 가능 (볼드, 이탤릭)

텍스트만 반환 (JSON 아님).
```

---

## Phase 5 — "오늘" 홈 화면

**목표**: 캘린더 대신 오늘 날짜 중심의 대시보드

### 레이아웃

```
┌────────────────────────────────────────────┐
│  2026년 4월 14일 (월요일)                  │
│  안녕하세요. 오늘의 기록을 시작해볼까요?  │
├────────────────────────────────────────────┤
│  오늘 기록 (2건)                           │
│  ┌──────────────────────┐ ┌─────────────┐ │
│  │ 일기 · 1번째         │ │ 별-01 · 3회 │ │
│  │ "오늘 처음으로..."   │ │ 2026-04-14  │ │
│  └──────────────────────┘ └─────────────┘ │
├────────────────────────────────────────────┤
│  최근 기록 (7일)                           │
│  04/13 · 아쉬운 점 1번째                  │
│  04/12 · 달-02 · 2회기                    │
│  04/10 · 일기 3번째                       │
├────────────────────────────────────────────┤
│  [+ 나의 기록 시작]    [+ 상담 기록 시작] │
└────────────────────────────────────────────┘
```

**구현**:
- `js/render-home.js` 신규 (캘린더 렌더러 대체)
- 캘린더는 왼쪽 사이드바 "캘린더" 탭으로 이동 (기존 `render-calendar.js` 재사용)
- `js/nav.js`의 `showHome()` 수정

---

## 구현 순서 및 우선순위

| 순위 | Phase | 핵심 파일 | 예상 규모 |
|------|-------|-----------|-----------|
| 1 | Phase 1 — 레이아웃 | render-sidebar.js, render-main.js, style.css | 대 |
| 2 | Phase 5 — 오늘 홈 | render-home.js (신규), nav.js | 중 |
| 3 | Phase 2 — 대화 흐름 | modal.js (신규), nav.js | 대 |
| 4 | Phase 3 — AI 프리셋 | state.js, render-forms.js | 소 |
| 5 | Phase 4 — 대화→일기 | chat-to-diary.js (신규), server.py | 중 |

---

## 전역 상태 변경 (state.js)

```js
// 추가/변경되는 state 키
{
  // 현재 메인 뷰
  mainView: 'home',   // 'home' | 'chat' | 'calendar'

  // 대화창 상태
  chatContext: null,  // { type: 'my'|'counsel', id: string } | null
  chatSession: [],    // 현재 대화 메시지 배열

  // 일기 변환
  selectedBlocks: [], // 선택된 대화 블록 인덱스 배열
  diaryDraft: null,   // AI 생성 일기 초안 (string | null)

  // 모달
  modalType: null,    // 'select-topic' | 'select-student' | null
}
```

---

## 데이터 구조 변경

기존 `Session.supervisionChat` → `Session.chat` (네이밍 통일, 마이그레이션 필요)

```js
// 마이그레이션 (loadData() 내 일회성 처리)
if (session.supervisionChat && !session.chat) {
  session.chat = session.supervisionChat;
}
```

새 `Session` 필드:
```js
chat: array,           // 대화 (역할: 'ai' | 'user')
chatCreatedAt: string, // 대화 시작 일시
```

---

## 테스트 계획

각 Phase 완료 후:
1. **백엔드**: 새 엔드포인트(`/api/chat-to-diary`) pytest 테스트 추가
2. **E2E**: `test_e2e.py`에 시나리오 추가
   - Phase 2: 버튼 클릭 → 주제 선택 모달 → 대화 시작 플로우
   - Phase 4: 메시지 선택 → 일기 변환 버튼 활성화 → 저장
3. **로깅 확인**: 각 Phase 기능에 logger.info 포인트 추가

---

## 설계 결정 사항

1. **캘린더 유지**: 완전 제거 아님 — 왼쪽 사이드바 "캘린더" 탭으로 접근 가능
2. **기존 데이터 호환**: `supervisionChat` → `chat` 필드 마이그레이션은 `loadData()`에서 자동 처리
3. **모달 vs 인라인**: 주제/내담자 선택은 중앙 대화창 위 오버레이 모달 (사이드바 점유 최소화)
4. **AI 역할 프리셋**: `state.js`의 상수로 관리. 사용자가 편집 불가 (고정 프리셋 + 커스텀 1개)
5. **대화 → 일기**: 새 레코드 생성 (덮어쓰기 아님). 주제 선택 모달에서 저장 위치 선택 가능
