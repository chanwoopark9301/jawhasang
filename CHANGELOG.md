# 변경사항 기록

## 작업 규칙

- 앞으로 기능 수정이나 구조 변경을 할 때는 먼저 이 파일에 변경사항을 기록한다.
- 기록 후 테스트를 실행하고, 통과한 변경만 커밋한다.
- 커밋 후 `origin/main`으로 푸시한다.

## 2026-05-08

### 투자 타임라인 뉴스 한국어 마크다운 정리

- 데스크/뉴스 수집으로 저장되는 투자 뉴스 이벤트를 타임라인에 넣기 전에 한국어 브리핑 형식으로 정규화하게 했다.
- 뉴스 본문을 `무슨 일이 있었나`, `왜 중요한가`, `확인할 것`, `원문 링크` 섹션으로 저장해 타임라인과 뉴스 목록에서 마크다운이 제대로 렌더링되게 했다.
- IREN AI 클라우드, SEC 공시, 스테이블코인/가상자산 법안, 금리·CPI, 반도체·나스닥 같은 투자 키워드는 한국어 표현과 보유 종목 관점의 확인 포인트로 변환한다.
- 가져온 뉴스가 RSS/일반 기사일 때는 원문 기사·공식 발표·가격/거래량 확인 전까지 약한 신호로 취급하도록 확인 문구를 붙였다.
- 서비스워커 캐시를 `jip-v121`, 정적 자산 버전을 `20260511-19`로 갱신했다.

### 투자 AI 첫 응답 원장 스냅샷 동기화

- 투자 대화 시스템 프롬프트를 만들기 전에 `/api/investment/ledger`로 정규화 원장 스냅샷을 먼저 읽어오게 했다.
- 서버에 `investment_accounts`·`investment_positions` 기준 원장 스냅샷 조회 API를 추가하고, 정규화 테이블이 비어 있으면 기존 앱 저장소로 폴백하게 했다.
- AI가 첫 대화에서 stale localStorage나 오래된 JSON 포트폴리오가 아니라 원장 DB 기준 보유 수량·현금·평단을 보도록 했다.
- 원장 동기화 성공/실패 로그를 추가해 AI 프롬프트의 계좌 기준이 어디서 왔는지 추적할 수 있게 했다.
- 서비스워커 캐시를 `jip-v120`, 정적 자산 버전을 `20260511-18`로 갱신했다.

### 대화 기반 포트폴리오 갱신 체감 속도 개선

- 포트폴리오 스냅샷 갱신 요청은 AI 호출 전에 뉴스·시세·환율 컨텍스트 조회를 생략하게 했다.
- 사용자가 수량·평단·현금 같은 원장 값을 직접 말한 경우 AI 왕복 없이 앱이 먼저 포트폴리오를 갱신하고, 서버 저장은 백그라운드로 처리하게 했다.
- 백그라운드 저장 완료·실패 로그를 추가해 느린 저장과 실제 반영 지연을 구분할 수 있게 했다.
- 저장이 1.2초 지연돼도 포트폴리오 화면 반영은 600ms 안에 끝나는 E2E와, 직접 갱신 요청이 `/api/analyze` 왕복 없이 처리되는 E2E를 추가했다.
- 서비스워커 캐시를 `jip-v119`, 정적 자산 버전을 `20260511-17`로 갱신했다.

### 포트폴리오 환율 동기화와 투자 대화 응답 안정화

- 현재가 갱신이 보유 종목과 지수뿐 아니라 `USDKRW=X` 환율도 함께 조회해 포트폴리오 원화 환산에 즉시 반영하게 했다.
- 시세 응답에 USD/KRW가 포함되면 `investment.usdKrwRate`와 갱신 시각/출처를 저장하게 했다.
- 투자 대화에서 “1달러당 1472원”처럼 사용자가 직접 말한 환율을 인식해 포트폴리오 화면과 환율 컨텍스트에 우선 반영하게 했다.
- 투자 대화 응답 길이를 늘려 포트폴리오 표와 계산이 중간에 잘리는 일을 줄였다.
- 환율 갱신 후 현금 `42135달러`가 `1472원` 기준 `62,022,720원`으로 표시되는 E2E 검증을 추가했다.
- 서비스워커 캐시를 `jip-v118`, 정적 자산 버전을 `20260511-16`으로 갱신했다.

### 투자 타임라인 가로 드래그 개선

- 투자 타임라인 그래프의 기본 폭을 넓혀 매매 포인트와 뉴스·일정·핵심 이벤트가 한 화면에 눌려 보이지 않도록 했다.
- 타임라인 그래프를 마우스나 터치로 잡고 좌우 드래그할 수 있게 했다.
- 가로 스크롤바 스타일과 드래그 중 커서를 정리해 차트 보드처럼 탐색하는 느낌을 강화했다.
- 타임라인 E2E에 그래프 폭, 가로 오버플로, 드래그 이동 검증을 추가했다.
- 서비스워커 캐시를 `jip-v117`, 정적 자산 버전을 `20260511-15`로 갱신했다.

### 초기 로딩 성능 개선

- 앱 시작 직후 서버 데이터를 이미 받은 경우, 오늘의 투자 데스크 자동 준비가 같은 `/api/data` 동기화를 즉시 반복하지 않도록 했다.
- 아침 자동 데스크 준비를 브라우저 idle 타이밍으로 미뤄 첫 화면, 포트폴리오 팝업, 채팅 입력의 초기 반응성을 먼저 확보하게 했다.
- 자동 데스크 준비에서는 사전 전체 저장을 생략하고 시세·일정·뉴스 수집 후 한 번만 저장하게 했다. 수동 `오늘 데스크 갱신`은 기존처럼 전체 동기화 경로를 유지한다.
- 데스크 준비 완료 로그에 백그라운드 실행 여부와 소요 시간을 남겨 다음 병목을 추적하기 쉽게 했다.
- 시작 직후 백그라운드 데스크 준비가 중복 서버 동기화와 중복 저장을 하지 않는 E2E 검증을 추가했다.
- 서비스워커 캐시를 `jip-v116`, 정적 자산 버전을 `20260511-14`로 갱신했다.

### 채팅 입력 누락 방지

- AI가 답변 중일 때 새 메시지를 보내면 입력창을 비우고 조용히 무시하던 흐름을 막고, 입력값을 그대로 유지한 채 다시 보내도록 안내하게 했다.
- 한글 IME 조합 중 Enter가 전송 Enter로 오인되어 입력이 사라지는 문제를 막기 위해 `isComposing`/`keyCode 229` 가드를 추가했다.
- 채팅 디버그용 `console.log` 출력은 제거하고 필요한 정보만 앱 로거로 남기도록 정리했다.
- 응답 중 Enter와 한글 조합 Enter가 메시지를 유실하지 않는 E2E 검증을 추가했다.
- 서비스워커 캐시를 `jip-v112`, 정적 자산 버전을 `20260511-10`으로 갱신했다.

### 장 마감 대화 자동정리 토스트 제거

- 장 마감 이후 투자 대화 세션을 타임라인 요약 이벤트로 자동 정리하는 백그라운드 작업이 사용자 직접 저장처럼 토스트를 띄우던 문제를 제거했다.
- 자동 정리 결과는 타임라인에 조용히 저장하고, 개발 로그에만 남기도록 바꿨다.
- 장 마감 대화 자동정리가 이벤트는 만들되 토스트는 띄우지 않는 E2E 검증을 추가했다.
- 서비스워커 캐시를 `jip-v111`, 정적 자산 버전을 `20260511-09`로 갱신했다.

### 투자 원장 정규화 준비 및 타임라인 그래프 복원

- Supabase/PostgreSQL 사용 시 `investment_accounts`, `investment_positions`, `investment_transactions`, `investment_cash_ledger`, `investment_events` 정규화 테이블을 생성하고 투자 스냅샷을 테이블로 미러링하도록 했다.
- 종목 저장 API가 기존 JSON 저장과 함께 정규화 포지션 테이블에도 저장하도록 보강했다.
- `/api/investment/transactions`를 추가해 매매 기록을 서버에서 idempotency key로 중복 차단하고, 포지션 수량·평단·현금·실현손익을 한 번에 반영하도록 했다.
- AI 대화에서 매매 기록을 저장할 때 서버 원장 API를 우선 사용하고, 실패 시에만 기존 로컬 반영으로 대체하도록 바꿨다.
- 서버/일정 동기화 응답이 늦게 도착해도 더 최근에 수정된 로컬 포지션·현금 값은 stale 응답으로 덮어쓰지 않도록 포지션 병합 기준을 보강했다.
- 투자 타임라인 팝업을 더 크게 만들고, 매도 포인트 그래프를 타임라인 안에 유지하면서 가로 스크롤 가능한 차트 영역으로 바꿔 그래프가 모달 밖으로 넘치지 않게 했다.
- 매매 거래 중복 적용 방지 서버 테스트와 타임라인 그래프 모달 경계 E2E 검증을 보강했다.
- 서비스워커 캐시를 `jip-v110`, 정적 자산 버전을 `20260511-08`로 갱신했다.

### 오늘의 데스크 리서치 쿼리 동적화

- 오늘의 데스크가 CRCL/ETH/IREN/QLD 전용 체크리스트처럼 굳어지지 않도록, 보유 종목의 티커·회사명·자산유형·섹터 힌트·최근 일정/뉴스를 바탕으로 매일 리서치 쿼리를 새로 만들게 했다.
- Clarity/Genius Act, USDC, IREN AI 계약, 반도체·나스닥 같은 항목은 해당 노출이 있을 때만 붙는 힌트로 낮추고, AAPL/XOM 같은 다른 보유 종목으로 바뀌면 그 종목의 실적·공시·애널리스트·사업모델·원자재/거시 민감도를 조사하게 했다.
- AI 데스크 프롬프트도 고정 종목 프롬프트가 아니라 현재 보유 노출의 가격 변동 요인, 공식 확인 자료, 손절/비중축소/추격매수 금지 조건을 요구하도록 바꿨다.
- 오늘의 데스크 화면 문구에서 특정 종목명을 제거하고 “현재 보유 종목별 핵심 가격 변수” 중심으로 바꿨다.
- 동적 쿼리가 현재 보유 종목과 일정 이벤트를 반영하고 불필요한 크립토 법안 쿼리를 붙이지 않는 E2E 테스트를 추가했다.
- 서비스워커 캐시를 `jip-v109`, 정적 자산 버전을 `20260511-07`로 갱신했다.

### Supabase 저장/병합 안정성 점검

- Supabase/PostgreSQL 저장 구조를 점검한 결과, 현재 앱은 `app_storage` 단일 행에 전체 앱 상태를 암호화 JSON으로 저장하는 구조라 도메인 테이블 간 SQL N+1은 없지만, 전체 덩어리 저장과 클라이언트 병합 실패가 더 큰 위험임을 확인했다.
- 서버 저장층에서 매 요청마다 반복하던 테이블 준비와 `information_schema` 컬럼 타입 조회를 프로세스 캐시로 줄여 Supabase 연결 왕복을 줄였다.
- 서버 최신 데이터를 다시 가져올 때 저장 큐가 끝나기 전 stale 서버 데이터를 먼저 병합하지 않도록 `refreshDataFromServer()`가 기본적으로 대기하게 했다.
- 투자 이벤트, 매매 판단, 주문 의도, 저널, 대화 세션은 서버/로컬 양쪽에 새 항목이 있을 때 길이 비교로 한쪽을 버리지 않고 id 기준으로 병합하도록 바꿨다.
- 현재가 갱신 후 포트폴리오 가격 저장이 끝나기 전에 화면만 먼저 갱신하지 않도록 `refreshInvestmentMarketData()`가 저장 완료를 기다리게 했다.
- Supabase 메타데이터 캐싱 단위 테스트와 투자 상태 id 병합 E2E 테스트를 추가했다.
- 서비스워커 캐시를 `jip-v108`, 정적 자산 버전을 `20260511-06`으로 갱신했다.

### 오늘의 데스크 아침 배치 준비 루틴 추가

- `오늘의 데스크`를 매일 09:00 이후 앱 실행/재진입 시 자동 준비하는 루틴으로 확장했다.
- 데스크 준비 단계는 서버 원장 동기화, 최신 원장 저장, 증권사 잔고 동기화(연결 시), 현재가·지수·환율 갱신, 실적·거시 일정 동기화, 보유 종목/정책 뉴스 수집 순서로 실행된다.
- 대화로 입력되어 `manualPrice`가 붙은 종목도 현재가 갱신/아침 배치에서는 실제 조회가를 `currentPrice`에 반영해 포트폴리오 평가액이 낡은 수동 가격에 묶이지 않게 했다.
- CRCL, ETH, IREN, QLD/나스닥 노출이 있으면 Clarity/Genius Act, USDC, 크립토 정책, IREN AI 계약/희석, 반도체·나스닥 모멘텀, CPI/Fed, 이란·호르무즈, 미중 회담 관련 뉴스 쿼리를 자동 생성해 투자 타임라인에 저장한다.
- 오늘의 데스크 모달에 `오늘 데스크 갱신` 버튼과 아침 배치 단계별 상태 표시를 추가했다.
- 일정 동기화가 서버 투자 상태를 그대로 덮어쓰지 않고 원장 병합 경로를 타도록 바꿔, 원장과 연결된 기능들이 최신 포트폴리오를 공유하게 했다.
- 서비스워커 캐시를 `jip-v107`, 정적 자산 버전을 `20260511-05`로 갱신했다.

### 서버 현금 단독 응답의 보유 종목 덮어쓰기 방지

- 서버 동기화 응답이 `CASH`만 포함하고 브라우저에는 유효한 주식/코인 보유 종목이 남아 있는 경우, 서버 응답으로 로컬 보유 종목을 지우지 않고 병합해 보존하도록 했다.
- 이 보호 로직이 작동하면 브라우저 로그에 보존된 종목과 서버/로컬 포지션 수를 경고로 남기게 했다.
- `현금 100%`, `주식/코인 평가액 $0`, `최대 보유 CASH 100%`처럼 서버 지연 응답 때문에 포트폴리오가 현금만 남는 회귀를 막는 E2E 테스트를 추가했다.
- 서비스워커 캐시를 `jip-v106`, `data.js` 정적 자산 버전을 `20260511-04`로 갱신했다.

### 포트폴리오 스냅샷 갱신 최우선 보강

- 투자 대화에서 `포트폴리오 갱신/수정/현재 상태` 의도가 있으면 문장 안에 매도/매수 표현이 섞여 있어도 매매 기록 누적보다 현재 계좌 스냅샷 정정을 우선 적용하게 했다.
- 종목별 주변 문맥에서만 수량, 평단, 현재가를 추출하도록 바꿔 여러 종목을 한 번에 말해도 같은 숫자가 모든 종목에 잘못 적용되지 않게 했다.
- `$167,594.542.3%`처럼 금액과 비중이 붙어 들어온 현금 문장도 금액 부분을 복구해 반영하도록 숫자 파싱을 보강했다.
- 포트폴리오 스냅샷 갱신 성공/실패 경로에 브라우저 로그를 추가하고, 갱신 의도는 있으나 추출값이 없을 때 경고 로그를 남기게 했다.
- 포트폴리오 리포트의 상단 지표를 `총 평가액`, `현금`, `주식/코인 평가액`, `주식/코인 매입금`, `평가손익`, `투자 수익률`로 나눠 현금과 투자 성과가 섞여 보이지 않게 했다.
- 서비스워커 캐시를 `jip-v105`, 정적 자산 버전을 `20260511-03`으로 갱신했다.

### 오늘의 데스크 시장 브리핑 전환

- `오늘의 데스크`를 계좌/포트폴리오 요약 화면이 아니라 거시 변수와 보유 종목의 미시 변수를 연결하는 시장 브리핑 화면으로 재정의했다.
- CRCL은 실적 숫자보다 USDC 발행량, 금리, 스테이블코인/Clarity/Genius Act 흐름을 우선 확인하도록 하고, ETH는 코인판 위험선호 선행지표, IREN은 AI 계약·실적·BTC·희석 리스크, 반도체/나스닥은 추격 진입 손익비를 따로 브리핑하게 했다.
- 데스크 모달을 `거시 브리핑`, `미시 브리핑`, `내 계좌에 주는 의미`, `AI 브리핑 질문` 구조로 바꾸고, 브리핑 질문을 대화창에 바로 채울 수 있게 했다.
- 투자 AI 시스템 프롬프트도 오늘의 데스크를 포트폴리오 요약이 아닌 시장 브리핑 레이어로 쓰도록 갱신했다.
- 오늘의 데스크 알림 본문도 총 평가액 대신 시장 브리핑 헤드라인을 우선 보내도록 바꿨다.
- 서비스워커 캐시를 `jip-v104`, 정적 자산 버전을 `20260511-02`로 갱신했다.

### 투자 대화 반영 후 포트폴리오/데스크 모달 즉시 갱신

- 투자 대화에서 포트폴리오, 현금, 매매기록, 뉴스, 원칙을 반영한 뒤 저장 완료를 기다리고 투자 화면·왼쪽 메뉴·열려 있는 투자 모달을 같은 상태로 다시 계산하게 했다.
- `계좌·포트폴리오`, `오늘의 데스크`, `투자 타임라인` 팝업이 열린 상태에서도 대화로 갱신된 계좌 원장과 위험 신호를 즉시 다시 렌더링하도록 공통 갱신 루틴을 추가했다.
- 대화 기반 포트폴리오 갱신 후 왼쪽 사이드 메뉴 팝업이 예전 포트폴리오/데스크 값을 계속 보여주지 않도록 회귀 테스트를 추가했다.

### 투자 계좌 원장 기준 정비 및 오늘의 데스크 재구성

- 매매 판단/체결 기록은 `investment.decisions`를 단일 원장으로 사용하고, `investment.events`에는 뉴스·공시·일정·시장 신호 같은 외부 맥락만 저장하도록 분리했다.
- 대화 저장과 매매 게이트가 더 이상 같은 매매를 `trade`, `trade-note`, `alert` 이벤트로 복제하지 않게 하여 타임라인·그래프·포트폴리오 계산의 중복 적용 가능성을 줄였다.
- 캘린더는 별도 복제 이벤트 없이도 매매 원장(`decisions`)과 외부 맥락(`events`)을 함께 읽어 투자 점을 표시하고, 투자 항목 클릭 시 투자 타임라인으로 이동하도록 바꿨다.
- 오늘의 투자 데스크를 계좌 우선 구조로 재정비해 현금 비중, 최대 노출, 종목별 통제 모드, 데이터 공백, 금지 행동을 먼저 보여주도록 했다.
- `뉴스·일정` 사이드 메뉴를 제거하고 뉴스·공시·일정·시장 신호는 투자 타임라인 안에서 통합 관리하도록 정리했다.
- 죽은 `renderModalInvestmentTimeline` 중복 정의를 제거하고, 서비스워커 캐시를 `jip-v102`, 정적 자산 버전을 `20260508-11`로 갱신했다.

### 투자 타임라인 그래프와 중복 매매 적용 방지

- 투자 타임라인의 매도 포인트 그래프가 모달 밖으로 밀려나지 않도록 그래프/포인트 레이어 폭과 overflow를 정리했다.
- 대화나 수동 게이트에서 같은 종목, 행동, 수량, 가격의 매매 기록이 이미 있으면 포트폴리오와 타임라인에 다시 적용하지 않도록 중복 키를 추가했다.
- 같은 매도 요청 문장을 반복해서 입력해도 기존 매매 사유/요약과 비교해 중복 적용을 차단하도록 했다.
- 서비스워커 캐시를 `jip-v101`, 정적 자산 버전을 `20260508-10`으로 갱신했다.

### 하단 상태바 전역 제거 보강

- 하단 `목록/화면` 모바일 상태바가 어떤 화면에서도 다시 표시되지 않도록 `#mobile-nav`, `.mobile-nav` 전역 숨김 규칙을 추가했다.
- 패널 토글 코드에서 제거된 모바일 상태바 활성 상태 갱신 흔적을 정리했다.
- 서비스워커 캐시를 `jip-v100`, 정적 자산 버전을 `20260508-09`로 갱신했다.

### 캘린더 점 표시와 투자 제목 중복 정리

- 모바일 하단의 `목록/화면` 상태바 DOM을 제거해 캘린더 화면 하단이 불필요하게 가려지지 않도록 했다.
- 캘린더 날짜 셀에서는 일상/상담/투자 이벤트 제목을 렌더하지 않고, 색상 점과 초과 개수만 표시하도록 바꿨다.
- 투자 일정/매매 이벤트가 `IREN IREN 매도`처럼 종목명이 반복되어 보이지 않도록 캘린더 팝업과 투자 타임라인의 제목 표시를 정규화했다.
- 서비스워커 캐시를 `jip-v99`, 정적 자산 버전을 `20260508-08`로 갱신했다.

### 서비스워커 자동 새로고침 제거

- 앱 첫 진입 또는 배포 직후 서비스워커 `controllerchange` 이벤트가 발생할 때 `window.location.reload()`를 실행하던 코드를 제거했다.
- 새 서비스워커는 계속 활성화하되, 현재 작성 중인 대화/기록/포트폴리오 화면을 보존하고 다음 앱 진입 때 새 정적 자산이 자연스럽게 반영되도록 했다.
- 서비스워커 주석을 자동 새로고침 없는 업데이트 흐름에 맞게 정리했다.

### 사이드 메뉴 단순화 및 포트폴리오 누적 방지

- 기존 왼쪽 사이드바의 일상/상담 목록 렌더링을 제거하고, 일상·상담·투자별 핵심 작업 버튼만 남긴 작업 메뉴로 바꿨다.
- 오른쪽 AI 패널 DOM과 모바일 AI 탭을 제거해 메뉴가 양쪽에 중복으로 보이지 않게 했다.
- 투자 메뉴는 왼쪽 사이드바의 `오늘의 데스크`, `계좌·포트폴리오`, `투자 타임라인`, `뉴스·일정` 네 버튼으로 통합했다.
- 포트폴리오 팝업을 열 때 과거 매도 기록을 다시 합산해 현금 포지션을 재구성하던 자동 보정 호출을 제거했다.
- 과거 매도 현금 보정 함수는 아직 현금 반영이 안 된 매도 기록만 한 번 처리하는 방식으로 줄여, 현재 계좌 스냅샷을 덮어쓰지 않게 했다.
- 투자 대화에서 원화/달러/환율/현금 질문이 나오면 `USDKRW=X` 환율을 조회해 현금과 총 평가액의 원화 환산 컨텍스트를 AI에 전달하도록 했다.

### 일상/상담 기록 루틴 알림 추가

- 앱 공통 설정 `app_settings.reminders`를 추가해 일상/상담 기록 알림 여부, 알림 시간, 오늘 기록이 비어 있을 때만 알림 옵션을 저장하게 했다.
- 홈 화면에 `알림` 진입점을 추가하고, `기록 알림` 모달에서 알림 시간과 일상/상담 대상, 테스트 알림을 설정할 수 있게 했다.
- PWA 알림 권한이 허용되면 설정한 시간에 “오늘 기록하실 거 없나요?” 류의 일상/상담 기록 리마인더를 보내도록 예약한다.
- 알림 클릭 시 `?modal=reminder-settings` 또는 투자 데스크 URL 파라미터를 읽어 해당 모달을 열도록 시작 라우팅을 보강했다.
- 서비스워커 캐시를 `jip-v96`, 정적 자산 버전을 `20260508-06`으로 갱신했다.

### 투자 대화 장 기준 세션 저장

- 투자 대화를 단일 전역 기록이 아니라 미국장 날짜별 `chatSessions`로 저장하도록 바꿔, 앱을 나갔다 들어와도 같은 장 기준 대화를 이어갈 수 있게 했다.
- 기존 `investment.chat` 기록은 첫 세션으로 자동 이관하고, localStorage 키도 `jip_chat_v2_investment_market-YYYY-MM-DD` 형식으로 분리했다.
- 장 마감 이후에는 해당 세션의 대화를 투자 타임라인 `review` 이벤트로 자동 요약 저장하고, 장 마감 후 추가 대화가 생기면 같은 요약 이벤트를 갱신하도록 했다.
- 초기 데이터 로드 완료 시 장 마감 대화 정리 루틴을 함께 실행하도록 연결했다.
- 서비스워커 캐시를 `jip-v95`, 정적 자산 버전을 `20260508-05`로 갱신했다.

### 오늘의 데스크 PWA 알림 기반 추가

- 투자 상태에 `notifications` 설정을 추가해 오늘의 데스크 알림 허용 여부, 일일 알림 시간, 위험 신호/일정/데스크 요약 발송 옵션을 저장하게 했다.
- `오늘의 투자 데스크` 모달에 `알림 켜기`와 `테스트 알림` 버튼을 추가했다.
- 알림 권한이 허용되면 오늘의 위험 신호, 투자 일정, 금지 행동 수를 기준으로 PWA 로컬 알림을 만들고 다음 일일 알림을 예약한다.
- 서비스워커 알림 클릭 시 앱을 다시 열도록 `notificationclick` 경로를 보강했다.
- 서비스워커 캐시를 `jip-v94`, 정적 자산 버전을 `20260508-04`로 갱신했다.

### AI 대화 기반 포트폴리오 자동 갱신

- 투자 대화에서 `포트폴리오 갱신`, `보유 수량`, `평단`, `현재가`, `현금/예수금`을 말하면 매매 기록이 아니어도 실제 포트폴리오 값을 자동 반영하도록 했다.
- `현금은 1억 2천 5백`처럼 원화 단위로 말한 금액은 앱 환율 기준 USD 현금 포지션으로 환산해 저장한다.
- 대화로 반영된 포트폴리오 변경은 투자 타임라인에 `포트폴리오 자동 갱신` 이벤트로 남겨 추적할 수 있게 했다.
- 서비스워커 캐시를 `jip-v93`, 정적 자산 버전을 `20260508-03`으로 갱신했다.

### 투자 원칙의 오늘의 데스크 통합

- 오른쪽 투자 사이드바에서 `원칙·매매` 진입점을 제거하고, `투자 타임라인`을 직접 여는 메뉴로 바꿨다.
- 투자 원칙 요약과 수동 편집 도구를 `오늘의 투자 데스크` 안으로 이동해, 오늘의 금지 행동/허용 행동/위험 신호와 같은 화면에서 판단 기준을 확인하게 했다.
- 기존 `investment-rules` 경로는 호환성을 위해 오늘의 데스크를 열도록 연결해 두었다.
- 서비스워커 캐시를 `jip-v92`, 정적 자산 버전을 `20260508-02`로 갱신했다.

### 투자 타임라인 통합 및 매도 포인트 그래프

- 매매 기록, 뉴스 동향, 투자 신호, 일정 이벤트를 별도 팝업으로 흩어두지 않고 `투자 타임라인` 한 화면에서 시간순으로 보이도록 통합했다.
- 기존 매매 기록/뉴스 동향 진입점은 호환성을 유지하되 모두 통합 타임라인을 열도록 바꾸고, 수동 매매 추가와 뉴스 추가 폼은 타임라인 안의 접힌 도구로 숨겼다.
- 매도 기록은 타임라인 상단의 `매도 포인트 그래프`로 시각화하고, 각 매도 지점에 마우스를 올리면 수량, 체결가, 매도대금, 실현손익을 작은 툴팁으로 확인할 수 있게 했다.
- 뉴스/공시 본문과 매매 요약은 통합 타임라인 안에서도 기존 마크다운 렌더링을 유지하도록 테스트를 갱신했다.
- 서비스워커 캐시를 `jip-v91`, 정적 자산 버전을 `20260508-01`로 갱신했다.

## 2026-05-07

### 자산 데스크 전환 계획 및 레거시 코드 정리

- `ASSET_DESK_IMPLEMENTATION_PLAN.md`를 추가해 투자 파트너를 `자산` 축으로 확장하는 제품 정의, 데이터 구조, 디렉토리 방향, 단계별 구현 계획을 정리했다.
- 은행 API 직접 연동은 장기 과제로 두고, 초기에는 채팅창에 은행/카드/증권 입출금 문자 내역을 붙여넣으면 거래를 추출해 자산 장부에 반영하는 방향으로 설계했다.
- 사라진 투자 사이드 메뉴 버튼 selector와 `Search signals` 레거시 동적 삽입 훅을 제거했다.
- 투자 현금 보정 로직에서 중복 현금 생성 함수를 제거하고 기존 `setInvestmentCashAmount` 경로로 통일했다.
- 서비스워커 캐시를 `jip-v90`, 정적 자산 버전을 `20260507-17`로 갱신했다.

### 주식계좌식 예수금 처리 및 투자 메뉴 통합

- 매도 체결 시 손익 금액만 더하는 방식이 아니라 매도대금 전체를 예수금으로 반영하고, 실현손익은 별도 성과 지표로 기록하도록 정리했다.
- 매수/추가매수는 계좌 총액 안에서 예수금을 줄이고 보유 종목을 늘리는 흐름으로 유지해, 반복 대화나 중복 반영에도 포트폴리오 뼈대가 흔들리지 않게 했다.
- 투자 대화에서 매매를 기록할 때 요약에 예수금 변화와 실현손익을 함께 남기도록 보강했다.
- 오른쪽 투자 메뉴를 `오늘의 데스크`, `계좌·포트폴리오`, `원칙·매매`, `뉴스·일정` 네 묶음으로 통합하고, 투자 원칙/매매 기록/뉴스/타임라인/검색 신호/AI 비교는 허브 모달 안에서 접근하도록 바꿨다.
- 서비스워커 캐시를 `jip-v89`, 정적 자산 버전을 `20260507-16`으로 갱신했다.

### 계좌 총자산 기준 포트폴리오 배분 처리

- 투자 상태에 `account.totalCapital`을 추가해 포트폴리오가 단순 매매 이벤트 누적이 아니라 계좌 총액 안의 자산 배분으로 움직일 수 있게 했다.
- 현금이 있는 계좌에서 매수/추가매수를 실행하면 총자산을 늘리지 않고 현금을 줄이며 보유 수량을 늘리도록 바꿨다.
- 매도 실행 시에도 총자산을 새로 더하지 않고 매도 체결가 기준 총 계좌 가치를 잡은 뒤 남은 종목 평가액을 제외한 금액만 현금으로 맞추게 했다.
- 현금이 전혀 없는 단순 종목 입력/테스트 계좌는 기존처럼 종목 수량 수정 흐름을 유지해 과거 수동 입력 호환성을 보존했다.
- 매매 반영 직후 주문 초안 API 응답이 오래된 서버 투자 상태로 방금 수정한 포트폴리오를 덮어쓰지 않도록 로컬 positions/decisions/events/account를 보존해 병합하게 했다.
- 서비스워커 캐시를 `jip-v88`, 정적 자산 버전을 `20260507-15`로 갱신했다.

### 포트폴리오 매도 중복 반영 및 현금 집중도 보정

- 같은 `IREN 1,190주 매도 / 잔여 510주` 대화가 반복될 때 이미 510주가 남아 있으면 추가 매도로 다시 적용하지 않도록 방어했다.
- 매도 반영 시 체결 수량이 현재 보유 수량보다 커도 실제 보유 수량을 초과해 현금이 증가하지 않게 했다.
- 자동 생성된 `CASH`는 중복 매도 기록을 제거한 고유 매도 금액 기준으로 보정되게 했다.
- 포트폴리오 리포트의 최대 보유, 집중도, 성과 기여, 가격 미조회 목록에서 현금과 0주 종목을 일반 위험 종목처럼 다루지 않게 했다.
- 이미 0주로 잘못 밀린 종목도 최근 매도 요약에 `잔여 수량`이 있으면 해당 잔여 수량 기준으로 복구하도록 보정 로직을 추가했다.
- 서비스워커 캐시를 `jip-v87`, 정적 자산 버전을 `20260507-14`로 갱신했다.

### Daily Investment Desk 추가

- `INVESTMENT_DAILY_DESK_IMPLEMENTATION_PLAN.md`에 디렉토리 점검 결과와 구현 계획을 정리했다.
- `js/investment-desk.js`를 추가해 포트폴리오, 현금, 최근 매매, 오늘/다가오는 투자 일정, 기존 룰 엔진 경고를 하나의 데스크 스냅샷으로 계산하게 했다.
- 투자 메인 화면과 오른쪽 사이드 메뉴에 `오늘의 데스크` 진입점을 추가하고, 모달에서 총 평가액, 현금, 최대 비중, 금지 행동, 위험 신호, 투자 일정, 체크리스트를 한눈에 보이게 했다.
- 투자 AI 시스템 프롬프트에 `Daily Investment Desk guardrails`와 데스크 요약을 주입해 매수/추가매수/재진입 질문에 앞서 금지 행동과 이벤트 리스크를 먼저 확인하도록 했다.
- 데스크 모달과 프롬프트 주입을 검증하는 E2E 테스트를 추가했다.
- 서비스워커 캐시를 `jip-v86`, 정적 자산 버전을 `20260507-13`으로 갱신했다.

### 매매 기록 저장과 마크다운 렌더링 보강

- 매도 체결이 포트폴리오에 반영될 때 매도대금이 자동으로 USD 현금 포지션에 더해지도록 했다.
- 매수/추가매수 체결이 포트폴리오에 반영될 때 보유 현금에서 매수대금이 차감되도록 했다.
- 기존에 매도는 반영됐지만 현금이 빠진 과거 매매기록은 포트폴리오 팝업을 열 때 1회 현금 보정되도록 했다.
- `포트폴리오 수정/반영`처럼 사용자가 직접 매매기록이라는 말을 쓰지 않아도 AI 답변의 매도/잔여 수량/체결가를 읽어 포트폴리오에 반영하도록 했다.
- `잔여 수량: 1,700 - 1,190 = 510주` 같은 답변은 510주 매도가 아니라 기존 1,700주에서 510주가 남도록 1,190주 매도로 계산하게 했다.
- 실현익 계산식 `(61.07 - 46.06) × 1,190`에서 매도 체결가를 추론해 포트폴리오 현재가에 반영하도록 했다.
- 투자 AI 프롬프트에 “직접 수정 기능이 없다”고 후퇴하지 말고, 정보가 충분하면 반영 가능한 형태로 답하도록 명시했다.
- 투자 채팅과 매매기록/포트폴리오 저장 요청이 동시에 발생해 오래된 저장 payload가 최신 포트폴리오를 덮어쓰는 문제를 막기 위해 `saveData`를 순차 저장 큐로 변경했다.
- 투자 대화 내용은 메시지 추가 즉시 로컬 캐시에도 반영하고, 서버 응답이 로컬보다 오래된 투자 채팅/매매 상태를 가져오면 최신 로컬 상태를 보존하도록 했다.
- 대화로 매매기록을 저장해 포트폴리오가 바뀌면 메인 투자 화면과 이미 열린 포트폴리오 팝업을 즉시 다시 렌더링하도록 했다.
- 투자 대화에서 `매매기록에 기록해줘`, `익절`, `매도`, `수익 실현` 같은 자연스러운 한국어 저장 지시를 안정적으로 매매 기록으로 저장하도록 보강했다.
- 저장할 종목을 사용자 문장뿐 아니라 직전 AI 응답 내용까지 함께 보고 추론하도록 바꿔, `IREN` 같은 티커가 답변에만 있어도 매매 기록에 연결되게 했다.
- 대화로 저장한 매매기록에 수량 또는 비율과 체결가가 포함되면 포트폴리오 보유 수량과 현재가도 자동 반영되도록 했다.
- 투자 타임라인에서 제목에 이미 티커가 포함된 경우 `IREN IREN 실적 발표`처럼 중복 표시되지 않도록 정리했다.
- 대화 저장 매매기록의 제목에서 `미지정 미지정 매수 기록`처럼 종목 미지정 문구가 반복되지 않도록 저장 제목과 타임라인 제목 구성을 분리했다.
- 매매 기록 상세와 최근 판단 카드, 투자 타임라인 본문에 `renderMarkdownBasic`을 적용해 제목, 목록, 링크, 표가 제대로 보이도록 했다.
- 공통 마크다운 렌더러에 기본 표 렌더링을 추가하고 표 CSS를 보강했다.
- 서비스워커 캐시를 `jip-v85` 및 `20260507-12`로 올려 배포 후 최신 저장/렌더링/포트폴리오 반영 코드가 바로 적용되도록 했다.

### X 자동 감시 보류와 대화형 검색 신호 전환

- X API 자동 감시를 사용자 기본 흐름에서 숨기고, 투자 대화에서 필요할 때 검색 요청을 보내는 방식으로 방향을 바꿨다.
- 투자 대화가 `검색`, `찾아줘`, `X`, `트윗`, `thetechinvest`, `elon`, `cathie` 같은 요청을 뉴스/공시 검색 트리거로 인식하도록 확장했다.
- 검색 결과가 AI 프롬프트에 들어온 경우 “실시간 검색 기능이 없다”고 답하지 않고, 제공된 검색 결과를 근거로 해석하도록 프롬프트 규칙을 보강했다.
- 기존 X signal 모달은 `Search signals`로 바꿔 자동 동기화 대신 수동 출처 저장과 알림 권한만 남겼다.
- 서비스워커 캐시를 `jip-v79` 및 `20260507-06`으로 올려 배포 후 최신 UI가 바로 반영되도록 했다.

### X 시장 신호 워치리스트와 알림 기반 저장 흐름

- 투자 데이터에 `signals` 구조를 추가해 X 관심 계정, 키워드 필터, 마지막 동기화 시각을 저장하도록 했다.
- `X_BEARER_TOKEN`이 설정되어 있으면 `/api/investment/x/sync`로 관심 계정의 최근 게시글을 가져와 키워드가 맞는 항목을 `investment.events(type: signal)`로 저장한다.
- 투자 사이드 메뉴에 X signal 모달을 추가해 관심 계정 추가/삭제, 수동 X 링크 저장, X 동기화, 브라우저/PWA 알림 권한 요청을 한 곳에서 처리하게 했다.
- 저장된 signal 이벤트는 기존 투자 타임라인, 캘린더, AI 투자 프롬프트 컨텍스트에 함께 들어가도록 연결했다.
- 서비스워커 캐시를 `jip-v78` 및 `20260507-05`로 올려 모바일 PWA와 브라우저가 새 JS를 받도록 했다.

### 실적 분석형 투자 답변 구조 강화

- 투자 AI가 실적 이벤트를 답할 때 컨센서스 숫자를 단순 반복하지 않고, 컨센서스·옵션 기대 변동폭·애널리스트 코멘트·회사 공시·뉴스가 서로 어긋나는 지점을 비교하도록 했다.
- 스토리 주식은 EPS보다 AI Cloud 매출, ARR/RPO, 대형 고객 acceptance, ATM/유상증자, 희석 리스크, 루머 확인 여부를 우선 판정 변수로 삼도록 프롬프트를 보강했다.
- 애널리스트 컨센서스 이벤트에는 원본 수치 필드를 함께 저장해 이후 컨센서스 변화 감지와 투자 원칙 재점검 트리거로 확장할 수 있게 했다.

### 투자 일정 동기화 0건/500 오류 보정

- 외부 실적 캘린더 응답에 잘못된 날짜 값이 섞여도 `/api/investment/calendar/sync`가 500으로 실패하지 않도록 날짜 검증을 추가했다.
- Alpha Vantage 실적 캘린더 키가 없거나 누락될 때도 Google News/RSS의 실적 발표 보도자료 제목에서 `May 7` 같은 날짜를 추출해 보유 종목 실적 이벤트를 캘린더에 저장하도록 fallback을 추가했다.

### 투자 이벤트 캘린더와 컨센서스 동기화

- 투자 일정 동기화 기능을 추가해 보유 종목 실적 발표일, 미국 주요 경제지표 일정, 애널리스트 실적/목표주가 컨센서스를 `investment.events`에 저장하고 캘린더에 표시하게 했다.
- `Alpha Vantage` 실적 캘린더, `Trading Economics` 미국 경제 캘린더, `FMP` 애널리스트 컨센서스를 선택적으로 연결하는 서버 모듈을 추가했다.
- 투자 AI 프롬프트에 다가오는 실적·매크로·애널리스트 이벤트를 포함해, 이벤트 전 매매 계획을 물으면 저장된 일정과 컨센서스를 근거로 답하도록 했다.

### 투자 AI 답변 품질 기준 강화

- 투자 대화가 실제 투자금, 평단, 익절 비율, 실현익을 받으면 남은 원가, 남은 평가액, 미실현 손익, 총 이익, 세금 예비금을 먼저 계산하도록 프롬프트를 보강했다.
- 실적 발표와 같은 이벤트 질문에는 주가 예측 단정 대신 상승/중립/하락 시나리오별 행동 규칙, 체크포인트, 최종 액션 플랜으로 답하도록 했다.
- 포트폴리오 스냅샷을 투자 AI 시스템 프롬프트에 포함해 앱에 저장된 보유 종목과 총 평가손익을 답변 품질의 기본 근거로 쓰게 했다.

## 2026-05-06

### 한국투자증권 잔고/매매 기록 동기화 준비

- 조회 전용 `KIS` 브로커 어댑터를 추가해 `KIS_APP_KEY`, `KIS_APP_SECRET`, `KIS_CANO`, `KIS_ACNT_PRDT_CD`가 설정되면 한국투자증권 잔고와 국내 체결 내역을 가져올 수 있게 했다.
- `/api/investment/broker/sync` 엔드포인트를 추가해 동기화된 잔고를 포트폴리오에 병합하고 체결 내역을 투자 판단/타임라인 데이터로 저장하게 했다.
- 투자 메인/포트폴리오 팝업에 `KIS 동기화` 버튼을 추가하고, 키가 없을 때는 필요한 환경변수를 안내하도록 했다.

### 투자 AI의 역할 회피 응답 방지

- 투자 원칙, 비중 축소, 손절 조건, 추가매수 조건을 물었을 때 `제 역할 밖`으로 멈추지 않고 앱 기본 원칙과 보유 데이터를 근거로 `원칙 후보`와 `보수적 기본안`을 먼저 제시하도록 프롬프트를 보강했다.
- 비중 초과 상황에서는 사용자가 숫자를 직접 정하지 않아도 현재 한도와 보유 비중을 바탕으로 1차 목표 비중, 단계적 축소안, 예외 조건을 제안하게 했다.
- 매매 판단 모드에서도 비중, 수량, 축소폭, 추가매수 구간 질문을 조건부 매매 계획으로 바꿔 답하도록 역할 경계를 정리했다.

### 투자 메뉴의 직접 입력 폼 접기

- 투자 원칙, 매매 기록, 뉴스 동향 모달에서 직접 입력 폼을 기본 노출하지 않고 `수동 편집/수동 추가/수동 매매 점검` 토글 안으로 접었다.
- 투자 원칙 화면은 숫자 가드레일과 핵심 원칙, 진입/청산 기준, 금지 셋업, 복기 루틴을 먼저 읽는 구조로 바꿨다.
- 뉴스 동향과 매매 기록은 저장된 리스트를 먼저 보여주고, 직접 입력은 필요한 경우에만 열도록 해 AI 대화 중심 흐름을 유지했다.

### 투자 기본 원칙과 주문 연동 초안 저장

- 투자 파트너의 기본 원칙을 빈값 대신 보수적 트레이딩 가드레일로 채웠다. 기본값은 하루 손실 2%, 종목별 최대 비중 25%, 쿨다운 45분, 추격매수 제한 3%, 거래당 리스크 0.5%, 최소 손익비 2.5다.
- 핵심 원칙, 진입 체크리스트, 청산 기준, 금지 셋업, 복기 루틴을 디폴트로 제공해 사용자가 처음부터 모든 기준을 직접 정하지 않아도 AI가 같은 기준으로 대화하도록 했다.
- 주문 연동 전 단계로 `/api/investment/order-intent`가 주문 의도 초안을 데이터에 저장하도록 바꿨고, 매매 게이트가 통과된 거래는 포트폴리오 반영과 함께 주문 연동용 초안을 남기도록 했다.

### + 버튼 AI 모드 아코디언화

- 대화창 `+` 메뉴를 `일상 / 상담 / 투자` 아코디언 구조로 바꿔 공통 모드와 투자 전용 모드를 한 곳에서 선택할 수 있게 했다.
- 투자 섹션에 `상태`, `뉴스`, `원칙`, `매매`, `정리` 모드를 추가하고 각 모드가 투자 프롬프트에 직접 반영되도록 했다.
- 투자 상태 모드는 현재가/손익 컨텍스트를, 투자 뉴스 모드는 뉴스/공시 컨텍스트를 우선 조회하도록 연결했다.
- 투자 화면에 들어가면 기본 모드를 `투자 상태`로 맞추고, 다른 화면으로 나가면 투자 전용 모드가 일상 대화에 남지 않도록 했다.

### 투자 대화에서 보유 종목 시세 컨텍스트 자동 조회

- 투자 대화에서 “써클 상태 어때”, “현재가/시세/가격/보유 종목 어때”처럼 묻는 경우 관련 티커의 현재가를 `/api/market/quote`로 먼저 조회해 AI 시스템 프롬프트에 붙이도록 했다.
- 사용자가 티커 대신 “써클”, “이더리움”, “비트코인”처럼 말해도 보유 종목의 CRCL/ETH/BTC를 찾아 상태 조회 대상으로 잡도록 했다.
- 시세 컨텍스트가 제공된 경우 AI가 “실시간 시세 조회 기능이 없다”고 답하지 못하도록 투자 프롬프트 규칙을 보강했다.
- 보유 수량, 평단, 현재가, 평가손익, 목표가/손절가와의 거리, 포트폴리오 비중을 함께 전달해 상태 질문에 바로 원칙 기준으로 답할 수 있게 했다.

### Claude/OpenAI 투자 답변 비교와 주문 연동 준비

- 투자 사이드 메뉴에 `AI 비교`를 추가해 같은 투자 질문을 Claude와 OpenAI에 동시에 보내고 답변을 나란히 비교할 수 있게 했다.
- 서버에 `/api/investment/ai-compare`를 추가하고, OpenAI 키가 있으면 OpenAI 응답도 함께 받아오도록 provider 호출을 분리했다.
- 실제 증권사/투자 앱 연동을 대비해 주문 실행이 아닌 초안 상태의 주문 의도(order intent)를 만드는 `/api/investment/order-intent`와 `investment_broker.py` 어댑터 초안을 추가했다.

### 홈 초기 로딩 체감 속도 개선

- 앱 시작 스플래시가 전체 스크립트 로드를 오래 가리지 않도록 빠르게 사라지는 부트 타이머를 추가했다.
- 홈 HTML의 초기 문구를 실제 홈 진입 화면에 가깝게 바꿔 JS 렌더 전에도 빈 로딩 화면처럼 보이지 않게 했다.
- 서버 데이터 변경 감지용 시그니처가 전체 데이터를 문자열화하지 않도록 가벼운 카운트/마지막 항목 기반으로 바꿔 기록이 많아질 때 초기 렌더가 느려지는 병목을 줄였다.

### 원화 표시를 전체 전환에서 보조 환산으로 변경

- 포트폴리오 기준 통화는 USD로 고정하고, 원화는 주요 금액 아래의 보조 환산값으로만 표시하게 바꿨다.
- 원/달러 토글 버튼과 KRW 입력 변환을 제거해 통화 전환 때 포트폴리오 전체가 다시 렌더링되며 렉이 걸리는 문제를 없앴다.
- 종목 관리와 매매 저널 입력값은 모두 달러 기준으로 단순화하고, 현금도 USD 보유액 기준으로 저장한다.

### 투자 모듈 파일 구조 정리

- 비대해진 `render-investment.js`에서 금액 포맷터, 포트폴리오 계산, 폼 저장 액션을 각각 `investment-format.js`, `investment-portfolio.js`, `investment-actions.js`로 분리했다.
- 투자 화면 파일은 렌더링 중심으로 남겨 이후 동기화, 타임라인, AI 기반 저장 기능을 추가할 때 기존 일상/상담 코드에 영향이 덜 가도록 정리했다.

### 투자 데이터 동기화와 타임라인 개선

- 앱이 포커스를 되찾거나 다시 보일 때 `/api/data`를 no-store로 재조회해 다른 기기에서 저장한 투자 종목과 기록이 더 빨리 반영되도록 했다.
- 초기 로딩 시 로컬 캐시 렌더와 서버 응답 렌더가 같은 데이터면 중복 렌더를 건너뛰고, 로딩 시간을 로그로 남겨 병목을 추적할 수 있게 했다.
- 투자 사이드 메뉴에 타임라인 팝업을 추가해 뉴스, 매매 판단, 위험 신호, 대화로 저장한 매매 메모를 날짜순으로 모아 보게 했다.
- AI 대화에서 투자 원칙, 매매 기록, 뉴스 동향 저장을 요청하면 해당 저장소에 반영하고, 프롬프트에는 정보가 부족할 때 필요한 항목만 물어보도록 추가했다.

### 컨텍스트 선택 허브와 죽은 투자 메뉴 정리

- 상단 상태 버튼을 누르면 캘린더, 일상, 상담, 투자로 바로 이동할 수 있는 앱 허브가 뜨도록 정리했다.
- 투자 화면에서 오른쪽 현재 영역을 누르면 일상/상담 선택기가 아니라 포트폴리오 종목 관리로 연결되게 바꿨다.
- 더 이상 쓰지 않는 `investment-positions` 모달 라우트와 중복 종목 관리 렌더러를 제거했다.

### 투자 원칙과 매매 기록을 트레이더용 워크플로우로 개편

- 투자 원칙 메뉴를 단순 메모가 아니라 리스크 한도, 진입/청산 체크리스트, 금지 셋업, 복기 루틴을 관리하는 트레이딩 플랜 화면으로 개편했다.
- 매매 기록 메뉴를 주문 전 체크리스트, 셋업/타임프레임/손익비/무효화 조건을 남기는 거래 저널 구조로 확장했다.
- 원/달러 보기 전환 시 모달을 닫았다 다시 여는 흐름을 없애고, 열린 팝업 내용을 즉시 교체해 토글이 사라지는 느낌을 줄였다.

### 써클 CRCL 입력 정규화 보강

- 써클은 NYSE 상장 종목이므로 나스닥 여부와 무관하게 CRCL로 조회하도록 입력 정규화를 보강했다.
- `써클(CRCL)`, `Circle (CRCL)`, `써클 인터넷 그룹`처럼 한글/회사명과 괄호 티커를 섞어 입력해도 CRCL을 추출해 현재가 조회와 저장에 사용하게 했다.
- Yahoo quote API가 401로 막히거나 Stooq에 신규 상장 종목 가격이 없을 때 CRCL 같은 미국 주식을 StockAnalysis 페이지에서 보조 조회하도록 fallback을 추가했다.

### 투자 화면 원/달러 표시 전환과 사이드바 정리

- 포트폴리오 요약, 팝업, 종목 관리 입력 폼에 원/달러 보기 전환 버튼을 추가해 같은 보유 종목을 USD/KRW 기준으로 바꿔 볼 수 있게 했다.
- KRW 보기에서 종목 단가와 현금 보유액을 입력하면 내부 USD 기준 저장값으로 환산해 기존 현재가 API와 포트폴리오 계산 흐름을 유지하게 했다.
- 오른쪽 투자 사이드바에서 중복되던 현재가 갱신과 종목 관리 버튼을 제거하고, 포트폴리오 팝업 안에서 갱신과 종목 관리를 처리하도록 정리했다.

### 포트폴리오 팝업 현재가 갱신 버튼 추가

- 포트폴리오 리포트 팝업 상단에 현재가 갱신 버튼을 추가해 팝업 안에서도 바로 가격과 위험 신호를 다시 조회할 수 있게 했다.
- 현재가 갱신 완료 후 포트폴리오 팝업이 열린 상태라면 평가액, 현재가, 갱신 시각이 즉시 다시 렌더링되도록 보강했다.

### 코인·써클 현재가 조회 fallback 보강

- 코인 현재가 조회 실패 시 CoinGecko 가격 API로 ETH/BTC/SOL/XRP 등 주요 코인을 다시 조회한다.
- 써클·Circle 입력을 CRCL로 정규화하고 Yahoo search/chart 보조 조회를 추가해 신규 상장 종목 조회 실패를 줄인다.

### 포트폴리오 저장 병합과 표시 안정화 점검

- 종목 저장 후 기존 포트폴리오 항목이 사라지지 않도록 클라이언트/서버 병합 흐름을 점검한다.
- 포트폴리오 표시, 다중 종목 표시, 저장 직후 렌더링이 실제 저장 데이터와 일치하도록 보정한다.
- 현재가가 없는 종목도 포트폴리오 보유 목록에 별도로 노출해 저장 여부를 바로 확인할 수 있게 한다.

### 포트폴리오 종목 관리 접힘 처리와 저장 후 갱신

- 포트폴리오 팝업의 종목 추가 폼을 기본 접힘 토글로 숨겨 리포트 공간을 넓힌다.
- 종목 저장 전 조회된 현재가를 서버 저장 payload에 반영해 저장 직후 포트폴리오 차트와 총액이 최신 가격으로 갱신되게 한다.

### 주식·코인·현금 포트폴리오 확장

- CRCL처럼 Yahoo 현재가 조회가 비는 미국 주식은 Stooq fallback으로 한 번 더 조회한다.
- ETH, Ethereum, 이더리움 입력을 ETH-USD로 정규화해 코인도 포트폴리오에 등록할 수 있게 한다.
- 현금 보유액을 포트폴리오 안의 현금 자산으로 등록하고 총 평가액과 비중 계산에 포함한다.

### 포트폴리오 중심 종목 관리와 매매 반영

- 종목 추가·수정·삭제를 포트폴리오 팝업 안으로 통합한다.
- 매매 기록에 수량과 체결가를 입력하면 매수·추가매수·매도가 보유 수량과 평단에 자동 반영되도록 연결한다.
- 종목 관리 메뉴는 포트폴리오 관리 화면으로 이어지게 하고, 관련 E2E 테스트를 갱신한다.
### 뉴스 동향 팝업 확장과 마크다운 렌더링

- 뉴스 동향 팝업을 넓은 리포트형 레이아웃으로 확장하고 저장 현황, 관련 종목, 최근 기록일을 요약한다.
- 저장된 뉴스 본문을 기본 텍스트가 아니라 마크다운으로 렌더링해 AI가 정리한 제목·목록·링크가 그대로 보이게 한다.
- 뉴스 동향 E2E 테스트에 마크다운 표시와 확장 요약 지표 검증을 추가한다.
### 애널리스트형 포트폴리오 리포트 확장

- 포트폴리오 팝업에 집중도, 최대 보유 종목, 가격 갱신 상태, 위험 신호, 상승/하락 기여 종목을 추가한다.
- 단순 원형 차트 중심 화면에서 구성 진단과 현재 상태 요약을 함께 보는 리포트형 레이아웃으로 확장한다.
- 포트폴리오 E2E 테스트에 애널리스트형 상태 지표 표시 검증을 추가한다.
### 포트폴리오 평가액 계산과 팝업 개선

- 투자 수량·평단 입력이 쉼표 포함 숫자도 안전하게 파싱되도록 보강하고, 기존 문자열 숫자도 계산 전에 정규화한다.
- 포트폴리오 팝업을 넓히고 총 평가액, 총 매입금, 손익, 종목 수를 함께 보여주도록 개선한다.
- 차트 목록에 수량, 평단, 현재가, 평가액, 손익률을 표시해 계산 오류를 바로 확인할 수 있게 한다.
### 투자 뉴스 마크다운과 검색 품질 개선

- AI 채팅 마크다운 렌더러가 구분선, 일반 섹션 제목, 원문 링크 섹션을 더 자연스럽게 표시하도록 보강한다.
- 클래리티 법안·암호화폐 규제 뉴스 검색어를 복수 쿼리로 확장해 단일 RSS 결과에 과도하게 의존하지 않도록 개선한다.
- 투자 뉴스 답변 프롬프트가 확인된 자료와 추론을 구분하고, 빈약한 링크 나열 대신 핵심 사건·시장 영향·원칙 점검으로 답하도록 강화한다.
## 2026-05-05

### 투자 뉴스 일반 이슈 검색 지원

- 투자 뉴스 API가 종목 티커뿐 아니라 `query` 검색어도 받을 수 있게 하여, 클래리티 법안·GENIUS Act 같은 비종목 투자 이슈를 별도로 조회하도록 수정한다.
- 투자 AI 대화에서 "IREN 뉴스와 클래리티 법안 뉴스"처럼 종목과 규제 이슈가 섞인 요청을 각각 검색 컨텍스트로 전달하도록 보강한다.
- 뉴스 답변이 "실시간 검색 기능이 없다"로 빠지지 않도록 서버·프론트 테스트를 추가한다.

### 투자 종목 저장 500 진단 강화

- Supabase/PostgreSQL의 `jsonb` 컬럼이 `USER-DEFINED`로 감지되는 경우를 고려해 실제 `udt_name` 기준으로 저장 타입을 판별하도록 수정했다.
- 투자 종목 저장 API가 500을 반환할 때 `errorType`, `errorDetail`, `storageType`을 함께 내려 다음 배포 로그 없이도 원인 확인이 가능하게 했다.

### 캘린더 홈과 투자 뉴스 답변 개선

- 왼쪽 상단 `自畵像` 로고를 누르면 항상 캘린더 홈 화면으로 돌아오도록 수정했다.
- 캘린더 상단의 `일상/상담/투자` 허브 버튼을 제거하고, 날짜 칸 안에 노션형 이벤트 라벨로 기록을 표시하도록 바꿨다.
- AI 채팅 말풍선의 마크다운 렌더링을 복구하고, 투자 뉴스 응답이 단순 링크 나열에 그치지 않도록 프롬프트 컨텍스트를 강화했다.

### 투자 종목 저장 DB 호환성 수정

- 배포 DB의 `app_storage.data` 컬럼이 기존 JSON/텍스트 스키마여도 투자 종목 저장이 500으로 실패하지 않도록 저장소 읽기/쓰기 호환 처리를 추가했다.
- PostgreSQL 저장소의 실제 컬럼 타입을 로그에 남겨, 종목 저장 실패 시 DB 스키마 문제인지 바로 확인할 수 있게 했다.

## 2026-05-04

### 투자 API 진단과 캘린더 UI 정리

- 투자 종목 저장 API에 요청 ID, 티커, 저장 전후 개수, 예외 상세 로그를 추가해 배포 환경 저장 실패 원인을 추적할 수 있게 했다.
- 프론트 투자 API 호출에서 실패 응답 본문과 상태 코드를 함께 기록하고 사용자에게도 더 구체적인 실패 메시지를 보여주도록 정리했다.
- 메인 캘린더 허브에서 숫자 카운트를 제거하고, 왼쪽 캘린더 메뉴를 없애며, 캘린더 셀과 헤더를 더 작고 조밀하게 조정했다.

### 앱 구조와 최상위 메뉴 정리

- 투자 관련 백엔드 로직을 기존 상담/일상 서버 흐름에서 분리한 전용 모듈로 정리했다.
- 투자 관련 프론트 API 호출을 렌더링 코드에서 분리해 기존 앱 화면에 주는 영향을 줄였다.
- 최상위 메뉴 명칭을 `일상 / 상담 / 투자`로 바꾸고, 메인 화면을 캘린더 중심으로 전환했다.

### 투자 종목 전용 저장 API와 SEC 링크 보정

- 투자 종목 등록을 전체 데이터 저장이 아니라 전용 서버 API로 저장하도록 개선했다.
- SEC EDGAR 원문 링크가 개인정보 마스킹에 걸려 깨지는 문제를 수정했다.
- 뉴스 답변에서 오늘 날짜와 공시일을 혼동하지 않도록 AI 프롬프트 날짜 규칙을 강화했다.

### 투자 종목 등록 UX와 저장 재시도

- 종목 등록 화면에서 혼란을 주는 현재가 직접 입력칸을 제거했다.
- 투자 데이터 저장 시 서버 저장 요청을 자동 재시도해 일시적인 연결 실패에 덜 민감하게 만들었다.

### 투자 뉴스 검색 연결

- 투자 대화창에서 최신 뉴스 요청 시 서버가 여러 뉴스 소스를 조회해 AI 프롬프트에 포함하도록 추가했다.
- Polygon, Alpha Vantage, Finnhub, Benzinga API 키가 있으면 우선 사용하고, 키가 없으면 Yahoo Finance RSS와 Google News RSS fallback을 사용하도록 구성했다.
- 뉴스 결과에는 출처 신뢰도와 원문/요약 구분을 포함하고, 중복 링크/제목을 제거하도록 개선했다.

### 투자 저장 안정화와 포트폴리오 차트

- 투자 종목, 원칙, 매매 기록, 뉴스 저장 시 서버 DB 저장 응답을 기다린 뒤 UI를 닫도록 개선했다.
- 포트폴리오 메뉴를 팝업으로 열고, 보유 비중을 원형 차트와 종목별 비중 목록으로 보여주도록 추가했다.

### 수동 현재가 보존

- 사용자가 종목 등록 시 직접 입력한 현재가가 시장 API 조회값으로 덮어써지지 않도록 개선했다.
- 현재가 입력은 선택값으로 되돌리되, 비워두면 자동 조회하고 입력하면 수동 가격으로 표시하도록 조정했다.

### 종목 현재가 조회 안정화

- 종목 등록 시 현재가를 사용자가 직접 입력하지 않고 시장 데이터 조회로 채우도록 개선했다.
- 배포 환경에서 Yahoo quote API가 실패할 때 Yahoo chart API로 한 번 더 조회하는 fallback을 추가했다.
- 현재가 조회 실패 시 어떤 종목이 조회되지 않았는지 UI에서 더 분명히 알 수 있게 개선했다.

### 투자 파트너

- 투자 파트너를 기존 앱의 세 번째 작업 모드로 추가했다.
- 메인 화면을 포트폴리오 요약과 공통 AI 대화창 중심으로 개편했다.
- 종목 관리, 투자 원칙, 매매 기록, 뉴스 동향을 오른쪽 패널 메뉴와 모달로 분리했다.
- 투자 대화 내용을 `investment.chat`에 저장하도록 변경했다.
- 투자 원칙, 뉴스 동향, 매매 판단, 시장 데이터, 위험 신호를 `investment` 데이터 구조에 저장하도록 확장했다.
- `/api/market/quote` 서버 프록시를 추가해 보유 종목, NASDAQ, S&P 500 현재가를 조회할 수 있게 했다.
- 현재가 갱신 후 목표가 근접, 손절가 근접, 급등/급락, 비중 초과 위험 신호를 계산해 표시하도록 했다.

## 2026-05-11

### Portfolio snapshot table recovery

- Investment chat artifacts now try portfolio snapshot updates before saving content as investment rules.
- If the ledger only has `CASH`, the app can rebuild positions from a markdown table with `symbol | shares | avg price | current price`.
- Cash parsing now prefers explicit USD-converted cash and `USD/KRW` lines, so stale CASH values do not win over the user's latest account snapshot.
- Portfolio markdown accidentally pasted into core rules is stripped during portfolio updates and hidden from the portfolio report.
- Verified the markdown-table rebuild path, the existing snapshot update path, and the snapshot-over-trade-word priority path with E2E tests.
- Updated static asset cache to `20260511-11` and service worker cache to `jip-v113`.

## 2026-05-11

### Investment timeline event graph

- The investment timeline graph now plots key events alongside trade points, so news, signals, earnings, macro events, portfolio updates, and reviews appear on the same time axis as trades.
- Trade points keep the value/proceeds Y-axis, while non-trade events use stable event lanes with distinct colors and hover tooltips.
- Updated the timeline E2E coverage to assert that saved events are rendered as graph points, not only as list items.
- Updated static asset cache to `20260511-12` and service worker cache to `jip-v114`.

## 2026-05-11

### Investment desk news query hardening

- Daily desk news queries now strip markdown links, URLs, angle brackets, and overly long event bodies before calling the news API.
- The server news endpoint now sanitizes and skips malformed query fragments instead of failing the whole request with HTTP 400.
- Added backend and E2E coverage for long markdown/event-derived news queries.
- Updated static asset cache to `20260511-13` and service worker cache to `jip-v115`.
