# 변경사항 기록

## 작업 규칙

- 앞으로 기능 수정이나 구조 변경을 할 때는 먼저 이 파일에 변경사항을 기록한다.
- 기록 후 테스트를 실행하고, 통과한 변경만 커밋한다.
- 커밋 후 `origin/main`으로 푸시한다.

## 2026-05-06

### 투자 AI의 역할 회피 응답 방지

- 투자 원칙, 비중 축소, 손절 조건, 추가매수 조건을 물었을 때 `제 역할 밖`으로 멈추지 않고 앱 기본 원칙과 보유 데이터를 근거로 `원칙 후보`와 `보수적 기본안`을 먼저 제시하도록 프롬프트를 보강했다.
- 비중 초과 상황에서는 사용자가 숫자를 직접 정하지 않아도 현재 한도와 보유 비중을 바탕으로 1차 목표 비중, 단계적 축소안, 예외 조건을 제안하게 했다.
- 매매 판단 모드에서도 비중, 수량, 축소폭, 추가매수 구간 질문을 조건부 매매 계획으로 바꿔 답하도록 역할 경계를 정리했다.

### 투자 메뉴의 직접 입력 폼 접기

- 투자 원칙, 매매 기록, 뉴스 동향 모달에서 직접 입력 폼을 기본 노출하지 않고 `수동 편집/수동 추가/수동 매매 점검` 토글 안으로 접었다.
- 투자 원칙 화면은 숫자 가드레일과 핵심 원칙, 진입/청산 기준, 금지 셋업, 복기 루틴을 먼저 읽는 구조로 바꿨다.
- 뉴스 동향과 매매 기록은 저장된 리스트를 먼저 보여주고, 직접 입력은 필요한 경우에만 열도록 해 AI 대화 중심 흐름을 유지했다.

### 투자 기본 원칙과 주문 연동 초안 저장

- 투자 파트너의 기본 원칙을 빈값 대신 보수적 트레이딩 가드레일로 채웠다. 기본값은 하루 손실 2%, 종목별 최대 비중 25%, 쿨다운 45분, 추격매수 제한 3%, 거래당 리스크 0.5%, 최소 손익비 2.5다.
- 핵심 원칙, 진입 체크리스트, 청산 기준, 금지 셋업, 복기 루틴을 디폴트로 제공해 사용자가 처음부터 모든 기준을 직접 정하지 않아도 AI가 같은 기준으로 대화하도록 했다.
- 주문 연동 전 단계로 `/api/investment/order-intent`가 주문 의도 초안을 데이터에 저장하도록 바꿨고, 매매 게이트가 통과된 거래는 포트폴리오 반영과 함께 주문 연동용 초안을 남기도록 했다.

### + 버튼 AI 모드 아코디언화

- 대화창 `+` 메뉴를 `일상 / 상담 / 투자` 아코디언 구조로 바꿔 공통 모드와 투자 전용 모드를 한 곳에서 선택할 수 있게 했다.
- 투자 섹션에 `상태`, `뉴스`, `원칙`, `매매`, `정리` 모드를 추가하고 각 모드가 투자 프롬프트에 직접 반영되도록 했다.
- 투자 상태 모드는 현재가/손익 컨텍스트를, 투자 뉴스 모드는 뉴스/공시 컨텍스트를 우선 조회하도록 연결했다.
- 투자 화면에 들어가면 기본 모드를 `투자 상태`로 맞추고, 다른 화면으로 나가면 투자 전용 모드가 일상 대화에 남지 않도록 했다.

### 투자 대화에서 보유 종목 시세 컨텍스트 자동 조회

- 투자 대화에서 “써클 상태 어때”, “현재가/시세/가격/보유 종목 어때”처럼 묻는 경우 관련 티커의 현재가를 `/api/market/quote`로 먼저 조회해 AI 시스템 프롬프트에 붙이도록 했다.
- 사용자가 티커 대신 “써클”, “이더리움”, “비트코인”처럼 말해도 보유 종목의 CRCL/ETH/BTC를 찾아 상태 조회 대상으로 잡도록 했다.
- 시세 컨텍스트가 제공된 경우 AI가 “실시간 시세 조회 기능이 없다”고 답하지 못하도록 투자 프롬프트 규칙을 보강했다.
- 보유 수량, 평단, 현재가, 평가손익, 목표가/손절가와의 거리, 포트폴리오 비중을 함께 전달해 상태 질문에 바로 원칙 기준으로 답할 수 있게 했다.

### Claude/OpenAI 투자 답변 비교와 주문 연동 준비

- 투자 사이드 메뉴에 `AI 비교`를 추가해 같은 투자 질문을 Claude와 OpenAI에 동시에 보내고 답변을 나란히 비교할 수 있게 했다.
- 서버에 `/api/investment/ai-compare`를 추가하고, OpenAI 키가 있으면 OpenAI 응답도 함께 받아오도록 provider 호출을 분리했다.
- 실제 증권사/투자 앱 연동을 대비해 주문 실행이 아닌 초안 상태의 주문 의도(order intent)를 만드는 `/api/investment/order-intent`와 `investment_broker.py` 어댑터 초안을 추가했다.

### 홈 초기 로딩 체감 속도 개선

- 앱 시작 스플래시가 전체 스크립트 로드를 오래 가리지 않도록 빠르게 사라지는 부트 타이머를 추가했다.
- 홈 HTML의 초기 문구를 실제 홈 진입 화면에 가깝게 바꿔 JS 렌더 전에도 빈 로딩 화면처럼 보이지 않게 했다.
- 서버 데이터 변경 감지용 시그니처가 전체 데이터를 문자열화하지 않도록 가벼운 카운트/마지막 항목 기반으로 바꿔 기록이 많아질 때 초기 렌더가 느려지는 병목을 줄였다.

### 원화 표시를 전체 전환에서 보조 환산으로 변경

- 포트폴리오 기준 통화는 USD로 고정하고, 원화는 주요 금액 아래의 보조 환산값으로만 표시하게 바꿨다.
- 원/달러 토글 버튼과 KRW 입력 변환을 제거해 통화 전환 때 포트폴리오 전체가 다시 렌더링되며 렉이 걸리는 문제를 없앴다.
- 종목 관리와 매매 저널 입력값은 모두 달러 기준으로 단순화하고, 현금도 USD 보유액 기준으로 저장한다.

### 투자 모듈 파일 구조 정리

- 비대해진 `render-investment.js`에서 금액 포맷터, 포트폴리오 계산, 폼 저장 액션을 각각 `investment-format.js`, `investment-portfolio.js`, `investment-actions.js`로 분리했다.
- 투자 화면 파일은 렌더링 중심으로 남겨 이후 동기화, 타임라인, AI 기반 저장 기능을 추가할 때 기존 일상/상담 코드에 영향이 덜 가도록 정리했다.

### 투자 데이터 동기화와 타임라인 개선

- 앱이 포커스를 되찾거나 다시 보일 때 `/api/data`를 no-store로 재조회해 다른 기기에서 저장한 투자 종목과 기록이 더 빨리 반영되도록 했다.
- 초기 로딩 시 로컬 캐시 렌더와 서버 응답 렌더가 같은 데이터면 중복 렌더를 건너뛰고, 로딩 시간을 로그로 남겨 병목을 추적할 수 있게 했다.
- 투자 사이드 메뉴에 타임라인 팝업을 추가해 뉴스, 매매 판단, 위험 신호, 대화로 저장한 매매 메모를 날짜순으로 모아 보게 했다.
- AI 대화에서 투자 원칙, 매매 기록, 뉴스 동향 저장을 요청하면 해당 저장소에 반영하고, 프롬프트에는 정보가 부족할 때 필요한 항목만 물어보도록 추가했다.

### 컨텍스트 선택 허브와 죽은 투자 메뉴 정리

- 상단 상태 버튼을 누르면 캘린더, 일상, 상담, 투자로 바로 이동할 수 있는 앱 허브가 뜨도록 정리했다.
- 투자 화면에서 오른쪽 현재 영역을 누르면 일상/상담 선택기가 아니라 포트폴리오 종목 관리로 연결되게 바꿨다.
- 더 이상 쓰지 않는 `investment-positions` 모달 라우트와 중복 종목 관리 렌더러를 제거했다.

### 투자 원칙과 매매 기록을 트레이더용 워크플로우로 개편

- 투자 원칙 메뉴를 단순 메모가 아니라 리스크 한도, 진입/청산 체크리스트, 금지 셋업, 복기 루틴을 관리하는 트레이딩 플랜 화면으로 개편했다.
- 매매 기록 메뉴를 주문 전 체크리스트, 셋업/타임프레임/손익비/무효화 조건을 남기는 거래 저널 구조로 확장했다.
- 원/달러 보기 전환 시 모달을 닫았다 다시 여는 흐름을 없애고, 열린 팝업 내용을 즉시 교체해 토글이 사라지는 느낌을 줄였다.

### 써클 CRCL 입력 정규화 보강

- 써클은 NYSE 상장 종목이므로 나스닥 여부와 무관하게 CRCL로 조회하도록 입력 정규화를 보강했다.
- `써클(CRCL)`, `Circle (CRCL)`, `써클 인터넷 그룹`처럼 한글/회사명과 괄호 티커를 섞어 입력해도 CRCL을 추출해 현재가 조회와 저장에 사용하게 했다.
- Yahoo quote API가 401로 막히거나 Stooq에 신규 상장 종목 가격이 없을 때 CRCL 같은 미국 주식을 StockAnalysis 페이지에서 보조 조회하도록 fallback을 추가했다.

### 투자 화면 원/달러 표시 전환과 사이드바 정리

- 포트폴리오 요약, 팝업, 종목 관리 입력 폼에 원/달러 보기 전환 버튼을 추가해 같은 보유 종목을 USD/KRW 기준으로 바꿔 볼 수 있게 했다.
- KRW 보기에서 종목 단가와 현금 보유액을 입력하면 내부 USD 기준 저장값으로 환산해 기존 현재가 API와 포트폴리오 계산 흐름을 유지하게 했다.
- 오른쪽 투자 사이드바에서 중복되던 현재가 갱신과 종목 관리 버튼을 제거하고, 포트폴리오 팝업 안에서 갱신과 종목 관리를 처리하도록 정리했다.

### 포트폴리오 팝업 현재가 갱신 버튼 추가

- 포트폴리오 리포트 팝업 상단에 현재가 갱신 버튼을 추가해 팝업 안에서도 바로 가격과 위험 신호를 다시 조회할 수 있게 했다.
- 현재가 갱신 완료 후 포트폴리오 팝업이 열린 상태라면 평가액, 현재가, 갱신 시각이 즉시 다시 렌더링되도록 보강했다.

### 코인·써클 현재가 조회 fallback 보강

- 코인 현재가 조회 실패 시 CoinGecko 가격 API로 ETH/BTC/SOL/XRP 등 주요 코인을 다시 조회한다.
- 써클·Circle 입력을 CRCL로 정규화하고 Yahoo search/chart 보조 조회를 추가해 신규 상장 종목 조회 실패를 줄인다.

### 포트폴리오 저장 병합과 표시 안정화 점검

- 종목 저장 후 기존 포트폴리오 항목이 사라지지 않도록 클라이언트/서버 병합 흐름을 점검한다.
- 포트폴리오 표시, 다중 종목 표시, 저장 직후 렌더링이 실제 저장 데이터와 일치하도록 보정한다.
- 현재가가 없는 종목도 포트폴리오 보유 목록에 별도로 노출해 저장 여부를 바로 확인할 수 있게 한다.

### 포트폴리오 종목 관리 접힘 처리와 저장 후 갱신

- 포트폴리오 팝업의 종목 추가 폼을 기본 접힘 토글로 숨겨 리포트 공간을 넓힌다.
- 종목 저장 전 조회된 현재가를 서버 저장 payload에 반영해 저장 직후 포트폴리오 차트와 총액이 최신 가격으로 갱신되게 한다.

### 주식·코인·현금 포트폴리오 확장

- CRCL처럼 Yahoo 현재가 조회가 비는 미국 주식은 Stooq fallback으로 한 번 더 조회한다.
- ETH, Ethereum, 이더리움 입력을 ETH-USD로 정규화해 코인도 포트폴리오에 등록할 수 있게 한다.
- 현금 보유액을 포트폴리오 안의 현금 자산으로 등록하고 총 평가액과 비중 계산에 포함한다.

### 포트폴리오 중심 종목 관리와 매매 반영

- 종목 추가·수정·삭제를 포트폴리오 팝업 안으로 통합한다.
- 매매 기록에 수량과 체결가를 입력하면 매수·추가매수·매도가 보유 수량과 평단에 자동 반영되도록 연결한다.
- 종목 관리 메뉴는 포트폴리오 관리 화면으로 이어지게 하고, 관련 E2E 테스트를 갱신한다.
### 뉴스 동향 팝업 확장과 마크다운 렌더링

- 뉴스 동향 팝업을 넓은 리포트형 레이아웃으로 확장하고 저장 현황, 관련 종목, 최근 기록일을 요약한다.
- 저장된 뉴스 본문을 기본 텍스트가 아니라 마크다운으로 렌더링해 AI가 정리한 제목·목록·링크가 그대로 보이게 한다.
- 뉴스 동향 E2E 테스트에 마크다운 표시와 확장 요약 지표 검증을 추가한다.
### 애널리스트형 포트폴리오 리포트 확장

- 포트폴리오 팝업에 집중도, 최대 보유 종목, 가격 갱신 상태, 위험 신호, 상승/하락 기여 종목을 추가한다.
- 단순 원형 차트 중심 화면에서 구성 진단과 현재 상태 요약을 함께 보는 리포트형 레이아웃으로 확장한다.
- 포트폴리오 E2E 테스트에 애널리스트형 상태 지표 표시 검증을 추가한다.
### 포트폴리오 평가액 계산과 팝업 개선

- 투자 수량·평단 입력이 쉼표 포함 숫자도 안전하게 파싱되도록 보강하고, 기존 문자열 숫자도 계산 전에 정규화한다.
- 포트폴리오 팝업을 넓히고 총 평가액, 총 매입금, 손익, 종목 수를 함께 보여주도록 개선한다.
- 차트 목록에 수량, 평단, 현재가, 평가액, 손익률을 표시해 계산 오류를 바로 확인할 수 있게 한다.
### 투자 뉴스 마크다운과 검색 품질 개선

- AI 채팅 마크다운 렌더러가 구분선, 일반 섹션 제목, 원문 링크 섹션을 더 자연스럽게 표시하도록 보강한다.
- 클래리티 법안·암호화폐 규제 뉴스 검색어를 복수 쿼리로 확장해 단일 RSS 결과에 과도하게 의존하지 않도록 개선한다.
- 투자 뉴스 답변 프롬프트가 확인된 자료와 추론을 구분하고, 빈약한 링크 나열 대신 핵심 사건·시장 영향·원칙 점검으로 답하도록 강화한다.
## 2026-05-05

### 투자 뉴스 일반 이슈 검색 지원

- 투자 뉴스 API가 종목 티커뿐 아니라 `query` 검색어도 받을 수 있게 하여, 클래리티 법안·GENIUS Act 같은 비종목 투자 이슈를 별도로 조회하도록 수정한다.
- 투자 AI 대화에서 "IREN 뉴스와 클래리티 법안 뉴스"처럼 종목과 규제 이슈가 섞인 요청을 각각 검색 컨텍스트로 전달하도록 보강한다.
- 뉴스 답변이 "실시간 검색 기능이 없다"로 빠지지 않도록 서버·프론트 테스트를 추가한다.

### 투자 종목 저장 500 진단 강화

- Supabase/PostgreSQL의 `jsonb` 컬럼이 `USER-DEFINED`로 감지되는 경우를 고려해 실제 `udt_name` 기준으로 저장 타입을 판별하도록 수정했다.
- 투자 종목 저장 API가 500을 반환할 때 `errorType`, `errorDetail`, `storageType`을 함께 내려 다음 배포 로그 없이도 원인 확인이 가능하게 했다.

### 캘린더 홈과 투자 뉴스 답변 개선

- 왼쪽 상단 `自畵像` 로고를 누르면 항상 캘린더 홈 화면으로 돌아오도록 수정했다.
- 캘린더 상단의 `일상/상담/투자` 허브 버튼을 제거하고, 날짜 칸 안에 노션형 이벤트 라벨로 기록을 표시하도록 바꿨다.
- AI 채팅 말풍선의 마크다운 렌더링을 복구하고, 투자 뉴스 응답이 단순 링크 나열에 그치지 않도록 프롬프트 컨텍스트를 강화했다.

### 투자 종목 저장 DB 호환성 수정

- 배포 DB의 `app_storage.data` 컬럼이 기존 JSON/텍스트 스키마여도 투자 종목 저장이 500으로 실패하지 않도록 저장소 읽기/쓰기 호환 처리를 추가했다.
- PostgreSQL 저장소의 실제 컬럼 타입을 로그에 남겨, 종목 저장 실패 시 DB 스키마 문제인지 바로 확인할 수 있게 했다.

## 2026-05-04

### 투자 API 진단과 캘린더 UI 정리

- 투자 종목 저장 API에 요청 ID, 티커, 저장 전후 개수, 예외 상세 로그를 추가해 배포 환경 저장 실패 원인을 추적할 수 있게 했다.
- 프론트 투자 API 호출에서 실패 응답 본문과 상태 코드를 함께 기록하고 사용자에게도 더 구체적인 실패 메시지를 보여주도록 정리했다.
- 메인 캘린더 허브에서 숫자 카운트를 제거하고, 왼쪽 캘린더 메뉴를 없애며, 캘린더 셀과 헤더를 더 작고 조밀하게 조정했다.

### 앱 구조와 최상위 메뉴 정리

- 투자 관련 백엔드 로직을 기존 상담/일상 서버 흐름에서 분리한 전용 모듈로 정리했다.
- 투자 관련 프론트 API 호출을 렌더링 코드에서 분리해 기존 앱 화면에 주는 영향을 줄였다.
- 최상위 메뉴 명칭을 `일상 / 상담 / 투자`로 바꾸고, 메인 화면을 캘린더 중심으로 전환했다.

### 투자 종목 전용 저장 API와 SEC 링크 보정

- 투자 종목 등록을 전체 데이터 저장이 아니라 전용 서버 API로 저장하도록 개선했다.
- SEC EDGAR 원문 링크가 개인정보 마스킹에 걸려 깨지는 문제를 수정했다.
- 뉴스 답변에서 오늘 날짜와 공시일을 혼동하지 않도록 AI 프롬프트 날짜 규칙을 강화했다.

### 투자 종목 등록 UX와 저장 재시도

- 종목 등록 화면에서 혼란을 주는 현재가 직접 입력칸을 제거했다.
- 투자 데이터 저장 시 서버 저장 요청을 자동 재시도해 일시적인 연결 실패에 덜 민감하게 만들었다.

### 투자 뉴스 검색 연결

- 투자 대화창에서 최신 뉴스 요청 시 서버가 여러 뉴스 소스를 조회해 AI 프롬프트에 포함하도록 추가했다.
- Polygon, Alpha Vantage, Finnhub, Benzinga API 키가 있으면 우선 사용하고, 키가 없으면 Yahoo Finance RSS와 Google News RSS fallback을 사용하도록 구성했다.
- 뉴스 결과에는 출처 신뢰도와 원문/요약 구분을 포함하고, 중복 링크/제목을 제거하도록 개선했다.

### 투자 저장 안정화와 포트폴리오 차트

- 투자 종목, 원칙, 매매 기록, 뉴스 저장 시 서버 DB 저장 응답을 기다린 뒤 UI를 닫도록 개선했다.
- 포트폴리오 메뉴를 팝업으로 열고, 보유 비중을 원형 차트와 종목별 비중 목록으로 보여주도록 추가했다.

### 수동 현재가 보존

- 사용자가 종목 등록 시 직접 입력한 현재가가 시장 API 조회값으로 덮어써지지 않도록 개선했다.
- 현재가 입력은 선택값으로 되돌리되, 비워두면 자동 조회하고 입력하면 수동 가격으로 표시하도록 조정했다.

### 종목 현재가 조회 안정화

- 종목 등록 시 현재가를 사용자가 직접 입력하지 않고 시장 데이터 조회로 채우도록 개선했다.
- 배포 환경에서 Yahoo quote API가 실패할 때 Yahoo chart API로 한 번 더 조회하는 fallback을 추가했다.
- 현재가 조회 실패 시 어떤 종목이 조회되지 않았는지 UI에서 더 분명히 알 수 있게 개선했다.

### 투자 파트너

- 투자 파트너를 기존 앱의 세 번째 작업 모드로 추가했다.
- 메인 화면을 포트폴리오 요약과 공통 AI 대화창 중심으로 개편했다.
- 종목 관리, 투자 원칙, 매매 기록, 뉴스 동향을 오른쪽 패널 메뉴와 모달로 분리했다.
- 투자 대화 내용을 `investment.chat`에 저장하도록 변경했다.
- 투자 원칙, 뉴스 동향, 매매 판단, 시장 데이터, 위험 신호를 `investment` 데이터 구조에 저장하도록 확장했다.
- `/api/market/quote` 서버 프록시를 추가해 보유 종목, NASDAQ, S&P 500 현재가를 조회할 수 있게 했다.
- 현재가 갱신 후 목표가 근접, 손절가 근접, 급등/급락, 비중 초과 위험 신호를 계산해 표시하도록 했다.
