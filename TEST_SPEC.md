# 自畵像 — 테스트 명세서

> 테스트는 앱의 계약이다. 각 테스트는 "이 동작은 반드시 보장된다"는 약속이다.
> 자동화 테스트 프레임워크 도입 전까지는 이 명세서를 기준으로 수동 검증한다.

---

## 1. 백엔드 테스트

### 1-1. 인증 (Authentication)

| ID | 테스트 항목 | 입력 | 기대 결과 |
|----|------------|------|-----------|
| B-AUTH-01 | 올바른 비밀번호로 로그인 | `password=APP_PASSWORD` | 세션 발급, `/` 리다이렉트 |
| B-AUTH-02 | 틀린 비밀번호로 로그인 | `password=wrong` | 로그인 폼 재표시, 에러 메시지 |
| B-AUTH-03 | 미인증 상태에서 `/api/data` GET | 세션 없음 | `/login` 리다이렉트 (302) |
| B-AUTH-04 | 미인증 상태에서 `/api/analyze` POST | 세션 없음 | `/login` 리다이렉트 (302) |
| B-AUTH-05 | 로그아웃 | `/logout` GET | 세션 초기화, `/login` 리다이렉트 |
| B-AUTH-06 | 로그아웃 후 `/api/data` 접근 | 세션 없음 | `/login` 리다이렉트 |
| B-AUTH-07 | 이미 로그인된 상태에서 `/login` GET | 세션 있음 | `/` 리다이렉트 |

### 1-2. 데이터 암호화/복호화 (Encryption)

| ID | 테스트 항목 | 입력 | 기대 결과 |
|----|------------|------|-----------|
| B-ENC-01 | 데이터 저장 후 파일 읽기 | 임의 dict | 파일/DB 내용이 평문이 아님 (바이너리) |
| B-ENC-02 | 저장 후 복호화 | Fernet 암호문 | 원본 dict와 동일 |
| B-ENC-03 | 잘못된 키로 복호화 시도 | 다른 APP_PASSWORD | `InvalidToken` → EMPTY 반환 |
| B-ENC-04 | 레거시 평문 JSON 마이그레이션 | 암호화 안 된 data.json | 자동 암호화 후 저장, 정상 반환 |
| B-ENC-05 | 빈 DB/파일에서 읽기 | row 없음 | EMPTY 구조 반환 |

### 1-3. 데이터 API

| ID | 테스트 항목 | 입력 | 기대 결과 |
|----|------------|------|-----------|
| B-DATA-01 | `/api/data` GET — 초기 상태 | 빈 저장소 | `{students:[], sessions:[], my_topics:[], my_records:[], aiResults:{}}` |
| B-DATA-02 | `/api/data` POST — 저장 | 유효한 JSON | `{ok: true}` 반환, 데이터 영속화 |
| B-DATA-03 | 저장 후 GET | 저장된 데이터 | 저장한 내용 그대로 반환 |
| B-DATA-04 | 비어있는 POST body | `{}` or `null` | 500 또는 EMPTY 저장 (에러 없음) |
| B-DATA-05 | 대용량 데이터 저장 | verbatim 10,000자 × 5 | 정상 저장/반환 (2분 내) |

### 1-4. AI 프록시

| ID | 테스트 항목 | 입력 | 기대 결과 |
|----|------------|------|-----------|
| B-AI-01 | ANTHROPIC_API_KEY 없을 때 | 키 미설정 | `{error: 'ANTHROPIC_API_KEY가 ecrk.env에 없습니다'}`, 500 |
| B-AI-02 | 일반 요청 (비스트리밍) | `stream: false` | Anthropic 응답 JSON 그대로 반환 |
| B-AI-03 | 스트리밍 요청 | `stream: true` | `text/event-stream` Content-Type, SSE 청크 |
| B-AI-04 | Anthropic API 오류 전파 | 잘못된 모델명 등 | Anthropic 에러 상태코드/본문 그대로 전달 |

### 1-5. 개인정보 보호 (PII)

| ID | 테스트 항목 | 입력 | 기대 결과 |
|----|------------|------|-----------|
| B-PII-01 | 전화번호 패턴 스크러빙 | `010-1234-5678` 포함 텍스트 | `[전화번호]`로 마스킹 |
| B-PII-02 | 주민번호 패턴 스크러빙 | `901231-1234567` 포함 텍스트 | `[개인정보]`로 마스킹 |
| B-PII-03 | AI 페이로드 내 alias 사용 | AI 요청 payload | `alias` 값만 포함, 실명 없음 |
| B-PII-04 | 스크러빙 후 AI 호출 | PII 포함 텍스트 | 마스킹된 텍스트로 AI 호출 확인 |

---

## 2. 프론트엔드 테스트

### 2-1. 상태 관리 (State)

| ID | 테스트 항목 | 동작 | 기대 결과 |
|----|------------|------|-----------|
| F-STATE-01 | 초기 상태 | 앱 로드 | `state.view='student'`, `mode='welcome'`, 배열 초기화 |
| F-STATE-02 | 학생 추가 | `saveStudent()` | `state.students` 길이 +1 |
| F-STATE-03 | 학생 삭제 | `deleteStudent(id)` | 해당 학생 + 연결된 sessions 모두 제거 |
| F-STATE-04 | 회기 추가 | `saveSession()` | `sessionNum` 자동 할당, `state.sessions` +1 |
| F-STATE-05 | 회기 삭제 | `deleteSession(id)` | sessions에서 제거, selSession=null |
| F-STATE-06 | 뷰 전환 | `setView('myrecords')` | `state.view='myrecords'`, 선택 상태 초기화 |
| F-STATE-07 | 검색 쿼리 | `setSearchQuery('별')` | `state.searchQuery='별'` |
| F-STATE-08 | 태그 필터 토글 | `toggleFilterTag('진로')` | filterTags에 추가/제거 |

### 2-2. CRUD 검증

| ID | 테스트 항목 | 동작 | 기대 결과 |
|----|------------|------|-----------|
| F-CRUD-01 | 학생 등록 필수값 누락 | alias 없이 제출 | 폼 내 경고 표시, 저장 안 됨 |
| F-CRUD-02 | 회기 등록 필수값 누락 | date 없이 제출 | 경고, 저장 안 됨 |
| F-CRUD-03 | 회기 등록 필수값 누락 | verbatim 없이 제출 | 경고, 저장 안 됨 |
| F-CRUD-04 | sessionNum 자동 계산 | 3개 회기 후 새 회기 추가 | sessionNum=4 |
| F-CRUD-05 | 학생 수정 | `updateStudent()` | 원본 id 유지, alias 변경 |
| F-CRUD-06 | 주제 등록 필수값 누락 | title 없이 제출 | 경고, 저장 안 됨 |
| F-CRUD-07 | 주제 삭제 시 기록 연쇄 삭제 | `deleteTopic(id)` | 해당 topicId의 모든 records 제거 |
| F-CRUD-08 | recordNum 자동 계산 | 2개 기록 후 새 기록 추가 | recordNum=3 |

### 2-3. 렌더링

| ID | 테스트 항목 | 동작 | 기대 결과 |
|----|------------|------|-----------|
| F-REND-01 | XSS 방어 | alias에 `<script>alert(1)</script>` 입력 | 이스케이프 출력, 스크립트 미실행 |
| F-REND-02 | 마크다운 렌더링 | `**굵게**`, `*기울임*` 입력 | `<strong>`, `<em>` 렌더 |
| F-REND-03 | 축어록 파싱 | `상담사: 안녕` 형식 텍스트 | counselor/client 클래스 분리 렌더 |
| F-REND-04 | 보고서 섹션 토글 | 섹션 헤더 클릭 | `.rpt-collapsed` 클래스 토글 |
| F-REND-05 | 캘린더 점 표시 | 날짜에 session 2개, record 1개 | 주황 점 2 + 초록 점 1 표시 |
| F-REND-06 | 캘린더 최대 점 표시 | 날짜에 5개 기록 | 점 3개 + `+2` 표시 |
| F-REND-07 | 빈 상태 메시지 | 학생 없을 때 | 안내 메시지 표시 |

### 2-4. 나의 기록 AI — 역할 기반 동적 보고서

| ID | 테스트 항목 | 조건 | 기대 결과 |
|----|------------|------|-----------|
| F-MY-01 | 보고서 항목이 aiPrompt에 맞게 구성됨 | aiPrompt="운동 코치" | 섹션 라벨이 운동 관련 (운동 패턴, 개선 포인트 등) |
| F-MY-02 | overall 항목 항상 포함 | 어떤 aiPrompt든 | 마지막 섹션이 '종합 평가' |
| F-MY-03 | 기본 aiPrompt 없을 때 | aiPrompt="" | "따뜻하게 경청하고 성찰을 돕는 코치" 기본값 |
| F-MY-04 | 동적 섹션 렌더링 | AI가 4개 항목 반환 | 4개 섹션 렌더, savedAt/period 메타 제외 |
| F-MY-05 | 대화 시작 메시지 역할 반영 | aiPrompt="헬스 코치" | AI 첫 메시지가 헬스/운동 관련 어조 |
| F-MY-06 | 대화 시작 메시지 역할 반영 | aiPrompt="감정 일기 친구" | AI 첫 메시지가 감정 탐색 어조 |

### 2-5. 패널 접기/펼치기

| ID | 테스트 항목 | 동작 | 기대 결과 |
|----|------------|------|-----------|
| F-PANEL-01 | 사이드바 접기 | `◀` 버튼 클릭 | 사이드바 너비 0, `.collapsed` 클래스 |
| F-PANEL-02 | 사이드바 펼치기 | 접힌 상태에서 `▶` 클릭 | 원래 너비 복원 |
| F-PANEL-03 | AI 패널 접기 | `▶` 버튼 클릭 | AI 패널 너비 0, `.collapsed` 클래스 |
| F-PANEL-04 | AI 패널 펼치기 | 접힌 상태에서 `◁` 클릭 | 원래 너비 복원 |
| F-PANEL-05 | 접힘 상태 유지 | 페이지 새로고침 | localStorage에서 상태 복원 |
| F-PANEL-06 | 양쪽 모두 접기 | 두 패널 모두 접기 | 메인 영역이 전체 너비 차지 |

### 2-6. 검색 / 필터

| ID | 테스트 항목 | 동작 | 기대 결과 |
|----|------------|------|-----------|
| F-SEARCH-01 | 학생 검색 (alias) | `'별'` 입력 | alias에 '별' 포함된 학생만 표시 |
| F-SEARCH-02 | 학생 검색 (상황) | `'불안'` 입력 | situation에 '불안' 포함 학생 표시 |
| F-SEARCH-03 | 대소문자 무시 | `'STAR'` 입력 | 영문 포함 alias 검색 |
| F-SEARCH-04 | 태그 필터 | `'진로'` 태그 클릭 | 해당 태그 가진 회기만 표시 |
| F-SEARCH-05 | 태그 필터 해제 | 같은 태그 재클릭 | 전체 회기 다시 표시 |
| F-SEARCH-06 | 태그 필터 초기화 | `clearFilterTags()` | 전체 목록 표시 |

---

## 3. AI 프롬프트 기능 테스트

### 3-1. 슈퍼비전 보고서

| ID | 테스트 항목 | 조건 | 기대 결과 |
|----|------------|------|-----------|
| AI-SUP-01 | 보고서 생성 성공 | verbatim 있는 회기 | 5개 키 포함 JSON 반환 |
| AI-SUP-02 | 보고서 버튼 상태 | verbatim 없는 회기 | 버튼 비활성화(disabled) |
| AI-SUP-03 | 기존 보고서 재생성 | analysis 존재 | 버튼 텍스트 '보고서 재생성 ↗' |
| AI-SUP-04 | 스트리밍 진행 표시 | 생성 중 | 로딩 스피너 표시, 부분 텍스트 누적 |
| AI-SUP-05 | 생성 완료 후 탭 전환 | 생성 완료 | `sessionTab='report'`로 자동 이동 |
| AI-SUP-06 | alias 사용 확인 | 보고서 내용 | 실명 없음, alias만 사용 |
| AI-SUP-07 | JSON 파싱 실패 | AI가 비표준 응답 | `parseJSON()` fallback 동작 |

### 3-2. 슈퍼비전 대화

| ID | 테스트 항목 | 조건 | 기대 결과 |
|----|------------|------|-----------|
| AI-CHAT-01 | 대화 시작 | `startSupervisionChat()` | AI 첫 메시지 수신, 대화 탭으로 전환 |
| AI-CHAT-02 | 사용자 메시지 전송 | 텍스트 입력 후 전송 | 내 메시지 표시 → AI 응답 대기 → 응답 표시 |
| AI-CHAT-03 | Enter 전송 | Enter 키 | 메시지 전송 |
| AI-CHAT-04 | Shift+Enter 줄바꿈 | Shift+Enter | 줄바꿈, 전송 안 됨 |
| AI-CHAT-05 | 대화 초기화 | 지우기 버튼 클릭 | `supervisionChat=[]`, 빈 상태 |
| AI-CHAT-06 | 대화 중 로딩 | AI 응답 대기 중 | 입력창/버튼 비활성화 |
| AI-CHAT-07 | 이전 회기 맥락 포함 | 3회기 이후 대화 | 이전 회기 overall 요약 시스템 프롬프트 포함 |

### 3-3. 나의 기록 AI

| ID | 테스트 항목 | 조건 | 기대 결과 |
|----|------------|------|-----------|
| AI-MY-01 | 기간 선택 — 이번 주 | `myPeriod='week'` | 7일 이내 records만 포함 |
| AI-MY-02 | 기간 선택 — 이번 달 | `myPeriod='month'` | 당월 records만 포함 |
| AI-MY-03 | 기간 선택 — 전체 | `myPeriod='all'` | 전체 records 포함 |
| AI-MY-04 | aiPrompt 반영 | 커스텀 역할 설정 | 시스템 프롬프트에 aiPrompt 포함 |
| AI-MY-05 | 기본 aiPrompt | aiPrompt 비어있음 | "따뜻하게 경청하고 성찰을 돕는 코치" 기본값 |
| AI-MY-06 | 이전 보고서 요약 포함 | 기존 analysis 있음 | 이전 overall이 컨텍스트에 포함 |
| AI-MY-07 | 보고서 저장 | 생성 완료 | `record.analysis`에 저장, savedAt 기록 |

### 3-4. 전체 패턴 분석

| ID | 테스트 항목 | 조건 | 기대 결과 |
|----|------------|------|-----------|
| AI-PAT-01 | 상담 패턴 분석 | 3회기 이상 학생 | 6개 키 JSON (clientPattern, counselorPattern…) |
| AI-PAT-02 | 나의 기록 패턴 분석 | 3개 이상 기록 주제 | 6개 키 JSON (pattern, growth…) |
| AI-PAT-03 | 패턴 모달 표시 | 분석 완료 | `pattern-modal` 오버레이 표시 |
| AI-PAT-04 | 패턴 모달 닫기 | 모달 외부 클릭 또는 닫기 버튼 | 모달 숨김 |
| AI-PAT-05 | 기록 없을 때 버튼 상태 | sessions/records 없음 | 패턴 분석 버튼 비활성화 |

---

## 4. UI 흐름 테스트 (End-to-End)

### 4-1. 신규 학생 → 첫 상담 → 슈퍼비전 보고서

```
[흐름]
로그인 → 홈 캘린더 확인 → + 새 학생 클릭
→ 학생 등록 폼 (alias 입력) → 저장
→ 학생 선택됨, 목록 모드
→ + 회기 추가 클릭 → 회기 폼
→ 축어록 입력 → 저장
→ 회기 상세 (축어록 탭)
→ AI 패널 "슈퍼비전 보고서 생성 ↗" 클릭
→ 로딩 → 보고서 탭으로 자동 이동
→ 5개 섹션 확인 (각각 토글)
```

| 단계 | 기대 결과 |
|------|-----------|
| 학생 등록 | 사이드바에 새 학생 표시 |
| 회기 저장 | sessionNum=1, 상세 뷰 표시 |
| 보고서 생성 | analysis 5개 키, savedAt 저장 |
| 섹션 토글 | 클릭 시 접힘/펼침 |

### 4-2. 기존 학생 2회기 추가 → 슈퍼비전 대화

```
[흐름]
학생 선택 → + 회기 추가 → 2회기 저장
→ 회기 상세 → 슈퍼비전 보고서 생성
→ 대화 탭 클릭 → AI 첫 메시지 수신
→ 사용자 응답 입력 → 전송
→ 멀티턴 대화 3회 이상
→ 대화 초기화 확인
```

| 단계 | 기대 결과 |
|------|-----------|
| 2회기 | sessionNum=2 |
| AI 첫 메시지 | 핵심 장면 언급 + 질문 형식 |
| 멀티턴 | 이전 대화 맥락 유지 |
| 초기화 | `supervisionChat=[]` |

### 4-3. 나의 기록 — 일기 쓰기 → AI 코치 대화

```
[흐름]
상단 [나의 기록] 탭 클릭 → 뷰 전환
→ + 새 주제 → title='일기', aiPrompt='감정을 들어주는 친구처럼' 입력 → 저장
→ 주제 선택 → + 기록 추가 → 내용 입력 → 저장
→ 기록 상세 (본문 탭 — 마크다운 렌더 확인)
→ AI 패널 보고서 생성 → 보고서 탭 확인
→ 대화 탭 → AI 첫 메시지 → 답변 → 멀티턴
```

| 단계 | 기대 결과 |
|------|-----------|
| 주제 등록 | 사이드바에 주제 표시, AI 역할 패널에 표시 |
| 기록 저장 | 마크다운 렌더링 |
| 보고서 생성 | period 필드 포함 |
| AI 대화 | aiPrompt 역할 반영된 어조 |

### 4-4. 캘린더 흐름

```
[흐름]
홈 화면 캘린더 → 오늘 날짜 점 확인
→ 날짜 클릭 → 팝업 표시
→ 팝업 내 회기 항목 클릭 → 팝업 닫힘 + 해당 회기 상세로 이동
→ 팝업 내 '+ 상담 기록 추가' 클릭 → 새 회기 폼
→ 월 이동 (◀/▶) → 이전/다음 월 캘린더
```

| 단계 | 기대 결과 |
|------|-----------|
| 팝업 표시 | 해당 날짜 sessions + records 목록 |
| 항목 클릭 | 정확한 학생+회기 선택 상태 |
| 월 이동 | `calMonth` 업데이트, 점 재렌더 |

### 4-5. 학생/주제 수정 및 삭제

```
[흐름 — 수정]
학생 목록에서 ✏️ 클릭 → 수정 폼 (기존 값 pre-fill)
→ alias 수정 → 저장 → 사이드바 갱신 확인

[흐름 — 삭제]
학생 목록에서 🗑️ 클릭 → confirm 다이얼로그
→ 확인 → 학생 + 연결 회기 모두 제거
→ 사이드바에서 사라짐, sessions에서도 제거 확인
```

| 단계 | 기대 결과 |
|------|-----------|
| 수정 폼 열기 | 기존 값 pre-fill, mode='edit-student' |
| 저장 | id 유지, 변경값 반영 |
| 삭제 확인 | 학생 + sessions 동시 제거 |
| 삭제 취소 | 변경 없음 |

### 4-6. 패널 접기/펼치기 흐름

```
[흐름]
사이드바 ◀ 클릭 → 사이드바 접힘
→ 메인 영역 넓어짐 확인
→ 새로고침 → 접힌 상태 유지 확인
→ ▶ 클릭 → 사이드바 펼침
→ AI 패널 ▶ 클릭 → AI 패널 접힘
→ 양쪽 모두 접힘 → 메인 영역 전체 너비
```

| 단계 | 기대 결과 |
|------|-----------|
| 접기 | 패널 너비 0 (또는 아이콘 탭만) |
| 새로고침 후 | localStorage 상태 복원 |
| 메인 확장 | flex layout으로 자동 확장 |

### 4-7. 데이터 내보내기

```
[흐름]
사이드바 하단 내보내기 버튼 클릭
→ JSON 파일 다운로드
→ 파일 내용 확인: students, sessions, my_topics, my_records 포함
```

| 단계 | 기대 결과 |
|------|-----------|
| 다운로드 | `자화상_백업_YYYY-MM-DD.json` 파일 생성 |
| 파일 내용 | 4개 주요 키 포함, 구조 일치 |

### 4-8. 모바일 레이아웃

```
[흐름]
768px 이하 브라우저 또는 모바일 기기
→ 하단 네비게이션 표시 확인
→ '목록' 탭 → 사이드바 표시
→ '상담' 탭 → 메인 영역 표시
→ 'AI' 탭 → AI 패널 표시
```

| 단계 | 기대 결과 |
|------|-----------|
| 초기 | 하단 nav 표시, 사이드바 활성 |
| 탭 전환 | 해당 패널만 표시 (`mobile-active`) |

---

## 5. 회귀 테스트 체크리스트

기능 변경 후 반드시 확인할 항목:

- [ ] 로그인/로그아웃 정상 동작
- [ ] 학생 CRUD + sessions 연쇄 삭제
- [ ] 회기 저장 + sessionNum 자동 계산
- [ ] 슈퍼비전 보고서 생성 (스트리밍)
- [ ] 슈퍼비전 대화 멀티턴
- [ ] 나의 기록 CRUD
- [ ] 나의 기록 AI 보고서 생성
- [ ] 캘린더 점 표시 + 팝업
- [ ] 데이터 내보내기
- [ ] 패널 접기/펼치기 + 새로고침 후 상태 유지
- [ ] XSS: alias에 `<script>` 입력 시 이스케이프
- [ ] 모바일 하단 네비게이션

---

## 6. 성능 기준

| 항목 | 기준 |
|------|------|
| 앱 초기 로드 | 3초 이내 (서버 응답 포함) |
| AI 보고서 생성 | 30초 이내 (스트리밍 첫 청크 2초 이내) |
| 데이터 저장 (`/api/data` POST) | 1초 이내 |
| 캘린더 렌더링 | 200ms 이내 |
| 패널 리사이즈 드래그 | 60fps (jank 없음) |
