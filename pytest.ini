[pytest]
markers =
    e2e: Playwright E2E 브라우저 테스트 (실제 서버 필요, -m e2e 로 선택 실행)

# 기본 실행: 단위/통합 테스트만 (E2E 제외)
# pytest               → E2E 포함 전체 실행
# pytest -m "not e2e"  → 빠른 단위 테스트만
# pytest -m e2e        → E2E만 실행 (서버 기동 필요)
addopts = -v
