# 상담 일지 앱 — Claude Code 명세서

## 프로젝트 개요

중등 전문상담교사를 위한 상담 기록 + AI 슈퍼비전 웹 앱.
순수 HTML/CSS/JS (빌드 도구 없음). `server.py`(Python 프록시)를 띄운 뒤 브라우저에서 `index.html`을 열면 동작한다.

### 핵심 원칙

- **개인정보 보호**: 실명은 localStorage/서버에만 저장. AI API 호출 시 자동으로 가명(alias)으로 치환
- **오프라인 우선**: 인터넷 없어도 기록/조회 가능. AI 기능만 연결 필요
- **세 파일 구조**: `index.html` + `style.css` + `app.js`

---

## 파일 구조

```
counselingReport/
├── CLAUDE.md          ← 이 파일 (Claude Code 지시서)
├── index.html         ← HTML 구조
├── style.css          ← 스타일
├── app.js             ← 모든 로직 (상태관리, 렌더링, API 호출)
├── server.py          ← Python 프록시 서버 (Anthropic API 키 처리)
├── run.bat            ← 서버 실행 스크립트
├── requirements.txt   ← Python 의존성
├── data.json          ← 데이터 영속성 파일
└── ecrk.env           ← 환경변수 (API 키 등)
```

---

## 데이터 구조

### Student (학생)

```js
{
  id: string,          // 's' + Date.now()
  alias: string,       // 가명 (예: '별-01') — AI 호출에 사용, 실명 대체
  grade: string,       // '중1'~'중3', '고1'~'고3'
  gender: string,      // '남', '여', '' (미기재)
  family: string,      // 가족/가정환경 메모
  peers: string,       // 교우관계 메모
  situation: string,   // 현재 상황 메모
  notes: string,       // 기타 메모
  createdAt: string,   // 'YYYY-MM-DD'
}
```

### Session (상담 회기)

```js
{
  id: string,           // 'ss' + Date.now()
  studentId: string,    // Student.id 참조
  date: string,         // 'YYYY-MM-DD'
  sessionNum: number,   // 해당 학생의 몇 번째 회기 (자동 계산)
  verbatim: string,     // 상담 축어록 (전체 대화)
  memo: string,         // 상담사 메모
  analysis: object|null,      // 슈퍼비전 보고서 (AI 생성, 아래 참고)
  supervisionChat: array,     // 슈퍼비전 대화 내역
}
```

### 저장소

데이터는 `/api/data` (서버 → `data.json`)에 저장. 서버 미연결 시 localStorage 폴백.

---

## UI 레이아웃

```
┌─────────────────────────────────────────────────────────────┐
│  [학생별 | 날짜별]                                           │
│  사이드바(200px) │ 메인 컨텐츠(flex:1) │ AI 패널(255px)     │
│                  │                     │                     │
│  학생 목록       │ [축어록|보고서|대화] │ 학생 컨텍스트       │
│                  │                     │                     │
│  [+ 새 학생]     │         [+ 회기 추가]│ [슈퍼비전 보고서 ↗] │
└─────────────────────────────────────────────────────────────┘
```

### 탭 모드 (state.sessionTab)

| 탭         | 설명               |
| ---------- | ------------------ |
| `verbatim` | 축어록 입력/열람   |
| `report`   | AI 슈퍼비전 보고서 |
| `dialogue` | AI와 슈퍼비전 대화 |

---

## AI 슈퍼비전 기능

### 익명화 처리 규칙

AI API 호출 전 반드시 실명 대신 `student.alias` 사용. `student.name` 같은 실명 필드는 API payload에 절대 포함하지 않는다.

### 슈퍼비전 보고서 프롬프트 방침

- 각 항목은 **완성된 문장과 문단**으로 서술 (메모체 금지)
- 실제 발화 인용 시 따옴표로 감싸 문장 속에 자연스럽게 녹임
- 번호 목록 항목 앞에 반드시 `\n` 포함

### AI 보고서 응답 형식 (JSON)

```json
{
  "clientState": "내담자의 감정 흐름, 방어기제, 핵심 호소의 변화 (완성된 문단)",
  "techniques": "상담자가 사용한 기법들과 임상적 적절성 평가 (완성된 문장)",
  "strengths": "잘한 개입 3가지 이상 (각각 문단으로 서술)",
  "improvements": "개선이 필요한 장면 2-3개 (발화 인용 + 문제점 + 대안 응답)",
  "overall": "전반적 임상 평가와 다음 회기 핵심 과제 (완성된 글)",
  "savedAt": "YYYY-MM-DD"
}
```

### 보고서 UI — 토글 섹션

각 섹션은 클릭으로 접기/펼치기 가능. `▾` 아이콘이 방향으로 상태 표시.

| 키             | 색상 클래스  | 레이블            |
| -------------- | ------------ | ----------------- |
| `clientState`  | `rpt-blue`   | 내담자 상태 분석  |
| `techniques`   | `rpt-amber`  | 기법 분류 및 평가 |
| `strengths`    | `rpt-green`  | 잘한 점           |
| `improvements` | `rpt-red`    | 개선 포인트       |
| `overall`      | `rpt-purple` | 종합 슈퍼비전     |

토글: `.rpt-section`에 `.rpt-collapsed` 클래스 토글 → `.rpt-body` 숨김.

### 보고서 렌더링 규칙

```js
(a[s.key] || "—")
  .replace(/\n/g, "<br>") // 줄바꿈 처리
  .replace(/ (\d+)\)/g, "<br>$1)"); // 번호 항목 앞 자동 줄바꿈
```

---

## 전역 상태 (state 객체)

```js
const state = {
  view: "student", // 'student' | 'date'
  students: [], // Student[]
  sessions: [], // Session[]
  selStudent: null, // 선택된 Student.id
  selSession: null, // 선택된 Session.id
  mode: "welcome", // 뷰 모드
  sessionTab: "verbatim", // 'verbatim' | 'report' | 'dialogue'
  aiLoading: false,
  chatLoading: false,
  calYear: number,
  calMonth: number,
  calDate: null,
};
```

---

## 구현 시 주의사항

1. **실명 노출 금지**: API payload에 `student.name` 포함 절대 금지. `student.alias`만 사용.
2. **sessionNum 자동 계산**: `sessions.filter(s => s.studentId === id).length + 1`
3. **보고서 재생성**: 기존 `analysis`가 있으면 버튼 텍스트 '보고서 재생성 ↗'으로 표시.
4. **날짜별 뷰**: 전체 회기를 날짜 내림차순 정렬 후 월별 그룹핑.
5. **폼 취소**: `selStudent` 있으면 `'list'`, 없으면 `'welcome'`으로 복원.

---

## 향후 확장 포인트 (TODO)

- [ ] 회기 수정/삭제 기능
- [ ] 학생 정보 수정 기능
- [ ] 태그/카테고리 필터
- [ ] 전체 패턴 분석 (전체 상담 기록 기반)
- [ ] 데이터 내보내기 (JSON 백업)
- [ ] 검색 기능

---

## 개발 명령어

```bash
# 서버 실행 (AI 기능 포함)
run.bat   # 또는 python server.py

# 로컬에서 바로 열기 (AI 기능 제외)
open index.html
```

# 상담 일지 앱 — 추가 명세 (v2)

기존 CLAUDE.md 아래에 이어 붙일 것.

---

## 개발 배경

이 앱을 만든 사람은 중등 전문상담교사 임용을 준비 중인 기간제 상담교사다.

현재 두 가지 현실적 한계를 안고 일하고 있다.

첫째, 슈퍼비전 접근이 어렵다. 정교사가 아닌 기간제 신분이라 학회 가입이나 공식 슈퍼비전 체계에 접근하기 어렵다. 상담 실력이 부족하다는 걸 스스로 인식하고 있지만, 검증받을 구조가 없다.

둘째, 경험이 흩어진다. 현장에서 매일 크고 작은 것들을 느끼고 배우지만 — 이론대로 안 됐던 순간, 구조적으로 답답했던 것들, 공부하면서 막혔던 지점들 — 이것들이 기록되지 않으면 사라진다. 그런데 이 경험들이 나중에 대학원 연구의 재료가 되고, 한국 학교 상담 현실에 맞는 무언가를 만들고 싶다는 장기 목표의 씨앗이 된다.

이 앱은 그 두 가지 문제를 동시에 해결하려는 시도다.

---

## 개발 목적

### 단기 목적

- 상담 축어록 기반 AI 슈퍼비전으로 혼자서도 임상 성찰을 이어가기
- 임용 공부, 현장 관찰, 개인 일기 등 흩어진 경험을 한 곳에 누적하기

### 장기 목적

- 정교사 임용 후 대학원 진학 시, 쌓인 기록이 연구 재료가 되도록
- 한국 학교 상담 현실(입시 압박, 담임·학부모 개입, 슈퍼비전 부재 등)에 맞는 상담 방법론 또는 AI 도구를 연구하는 기반 마련
- 경험을 정리하고 누적하고 재정립하는 습관 자체를 만들기

### 핵심 설계 철학

> 누적이 데이터고, 유연성이 가치다.

상담 기록은 회기마다 쌓이고, 나의 기록은 주제마다 쌓인다.
AI는 그 누적된 데이터를 보고 패턴을 읽는다.
주제가 다양할 수 있으므로 AI 역할도 사용자가 직접 설정한다.

---

## 추가 기능 명세

### 1. 메인 화면 — 캘린더로 전면 교체

앱 첫 화면을 월별 캘린더로 교체한다. 기존 학생별/날짜별 뷰 전환 버튼 제거.

#### 캘린더 점 표시 규칙

날짜 셀 하단에 점(dot)으로 기록 유무와 개수를 표시.

- 초록 점 (`#1D9E75`) = 나의 기록 (my_records)
- 주황 점 (`#EF9F27`) = 상담 기록 (sessions)
- 점 개수 = 해당 날짜의 기록 수 (최대 3개 표시, 초과 시 `+N`)
- 두 종류 모두 있으면 초록/주황 나란히 표시

#### 날짜 클릭 → 팝업

날짜 클릭 시 해당 날짜 요약을 팝업(모달)으로 표시.

```
[4월 3일]
나의 기록 ●●              상담 기록 ●
──────────────────────────────────
· <일기> 3번째 기록        · 별-01 · 3회기
· <아쉬운 점> 1번째        · 달-02 · 2회기

[+ 나의 기록 추가]  [+ 상담 기록 추가]
```

각 항목 클릭 → 팝업 닫히고 해당 화면으로 전환.

---

### 2. 나의 기록 화면

#### 개요

상담 기록 화면과 레이아웃/구조 동일하게 유지.
색상 테마만 초록 계열로 변경.
별도 DB 키 `my_topics`, `my_records`로 상담 데이터와 완전 분리.

#### 데이터 구조

**Topic (주제)** — 상담의 Student에 해당

```js
{
  id: string,        // 't' + Date.now()
  title: string,     // 사용자 입력 (예: '일기', '아쉬운 점', '임용 공부')
  aiPrompt: string,  // 이 주제에서 AI가 맡을 역할 (사용자가 직접 입력)
  createdAt: string, // 'YYYY-MM-DD'
}
```

**Record (기록)** — 상담의 Session에 해당

```js
{
  id: string,            // 'r' + Date.now()
  topicId: string,       // Topic.id 참조
  date: string,          // 'YYYY-MM-DD'
  recordNum: number,     // 해당 주제의 몇 번째 기록 (자동 계산)
  content: string,       // 본문 (자유 서술, 마크다운 렌더링)
  memo: string,          // 짧은 메모 (선택)
  analysis: object|null, // AI 보고서 (기간 단위)
  aiChat: array,         // AI 대화 내역
}
```

#### 저장소

`/api/data` 응답에 `my_topics`, `my_records` 키 추가.

```python
empty = {
  'students': [],
  'sessions': [],
  'aiResults': {},
  'my_topics': [],   # 추가
  'my_records': [],  # 추가
}
```

#### 레이아웃

```
┌────────────────────────────────────────────────────────┐
│  사이드바(200px) │ 메인(flex:1)       │ AI 패널(255px) │
│                  │                    │                 │
│  주제 목록       │ [본문|보고서|대화] │ 주제 정보       │
│  · 일기          │                    │ aiPrompt 표시   │
│  · 아쉬운 점     │                    │                 │
│  [+ 새 주제]     │       [+ 기록 추가]│ [보고서 생성 ↗] │
└────────────────────────────────────────────────────────┘
```

#### 탭 구성

| 탭         | 설명                        |
| ---------- | --------------------------- |
| `content`  | 기록 본문 (마크다운 렌더링) |
| `report`   | AI 분석 보고서 (기간 단위)  |
| `dialogue` | AI와 대화                   |

#### UX — 일기 쓰기 경험

- 폰트: serif 계열 또는 가독성 좋은 sans-serif, 줄간격 1.8
- 입력창: 테두리 최소화, 넓은 여백, 타자 치는 느낌
- 저장된 본문: 마크다운 렌더링 (볼드, 이탤릭, 줄바꿈)
- 날짜·회차는 상단에 작게, 본문이 주인공이 되도록
- 색상 포인트: 초록 계열 (`#1D9E75`, `#0F6E56`)

#### 새 주제 등록 폼

```
주제 이름     [                              ]
AI 역할 설정  [이 주제에서 AI가 어떤 역할을
              해줬으면 하는지 자유롭게 입력.
              예: "나의 하루를 들어주고 감정을
              정리하도록 도와주는 친구처럼"
              예: "임용 공부 중 막히는 지점을
              같이 생각해주는 학습 코치처럼"  ]
              (비워두면 기본값 적용)
```

---

### 3. AI 기능 — 나의 기록

#### 핵심 원칙

- 보고서는 개별 기록이 아니라 **기간 묶음** 단위로 생성
- 누적된 기록 전문을 AI에게 보내는 것이 핵심
- 기록이 많아 토큰 한계에 걸릴 경우: 기간 이전 기록은 이전 보고서 요약으로 압축

#### AI에게 보내는 컨텍스트 구조

```
1. topic 정보
   - 주제 이름 + aiPrompt

2. 기간 이전 보고서 요약 (있으면)
   - "지난 분석 요약: [이전 보고서 overall 필드]"

3. 이번 기간 전체 기록 원문
   - 사용자가 선택한 기간의 모든 기록 전문
   - 형식: "N번째 기록 (YYYY-MM-DD):\n[content]\n[memo]"
```

#### 보고서 생성 UI

보고서 탭에서 기간 선택 후 생성:

```
기간 선택: [이번 주] [이번 달] [전체] [직접 선택]
                                      [보고서 생성 ↗]
```

#### 시스템 프롬프트

```
당신은 [aiPrompt] 역할입니다.
aiPrompt가 비어있으면: "따뜻하게 경청하고 성찰을 돕는 코치"

[이전 분석 요약 — 있으면 포함]
지난 분석 요약: ...

[이번 기간 기록 전문]
기간: YYYY-MM-DD ~ YYYY-MM-DD
N번째 기록 (날짜):
...

위 기록들을 분석하여 보고서를 작성해주세요.

규칙:
- aiPrompt의 역할에 맞게 보고서 항목을 스스로 구성할 것
- 반드시 포함할 항목: 패턴 요약 / 잘 된 것 / 개선점 / 다음 기간을 위한 질문
- 각 항목은 완성된 문장과 문단으로 서술
- 실제 기록 내용을 인용하며 근거 제시
- JSON으로만 응답 (다른 텍스트 없이)

JSON 구조 (항목명은 주제에 맞게 자유롭게 변경 가능):
{
  "pattern": "이 기간 기록에서 발견된 반복 패턴 (완성된 문단)",
  "strengths": "잘 된 것 또는 성장한 부분 (완성된 문단)",
  "improvements": "개선이 필요한 부분 또는 놓친 것 (완성된 문단)",
  "questions": "다음 기간 전 스스로에게 물어볼 질문 2-3개 (문단으로)",
  "overall": "전반적 평가와 다음으로 나아갈 방향 (완성된 글)",
  "savedAt": "YYYY-MM-DD",
  "period": "YYYY-MM-DD ~ YYYY-MM-DD"
}
```

#### 주제 유형별 보고서 방향

AI가 aiPrompt를 읽고 자율 적용. 새 주제 등록 폼에 아래 예시를 힌트로 제공할 것.

| 주제 유형 | aiPrompt 예시                  | 보고서가 자연스럽게 다루는 것        |
| --------- | ------------------------------ | ------------------------------------ |
| 학습 일기 | "임용 공부 메타인지 코치"      | 학습 패턴, 회피 습관, 다음 달 계획   |
| 개인 일기 | "감정을 함께 들여다보는 친구"  | 감정 흐름, 역기능적 신념, 공감       |
| 현장 관찰 | "학교 상담 연구자 관점의 동료" | 반복되는 구조적 문제, 연구 주제 후보 |
| 아쉬운 점 | "성장을 돕는 성찰 코치"        | 실수 패턴, 재발 방지, 격려           |

#### AI 대화 시스템 프롬프트

```
당신은 [aiPrompt] 역할입니다.

아래는 '[topic.title]' 주제로 작성된 기록입니다.
[현재 기록 본문 + 메모]
[보고서가 있으면 함께 포함]

대화 원칙:
- 상대방의 말을 충분히 듣고 반영하기
- 판단하지 않기
- 스스로 답을 찾도록 질문으로 안내하기
- 한국어 존댓말 사용
```

대화 시작 첫 메시지 트리거:

```
이 기록을 읽었어요. 지금 이 순간 가장 마음에 걸리는 게 뭔지 먼저 물어봐주세요.
```

---

### 4. 색상 테마 분리

| 구분                  | 포인트 색       | 용도                    |
| --------------------- | --------------- | ----------------------- |
| 상담 기록             | teal `#0F6E56`  | 기존 유지               |
| 나의 기록             | green `#1D9E75` | 사이드바, 탭, 버튼 강조 |
| 캘린더 — 나의 기록 점 | `#1D9E75`       | 초록 점                 |
| 캘린더 — 상담 기록 점 | `#EF9F27`       | 주황 점                 |

---

### 5. 전역 상태 추가

```js
// 기존 state에 아래 추가
myTopics: [],
myRecords: [],
selTopic: null,
selRecord: null,
myMode: 'welcome',  // 'welcome'|'list'|'detail'|'new-topic'|'new-record'
myTab: 'content',   // 'content'|'report'|'dialogue'
myAiLoading: false,
myChatLoading: false,
calPopup: null,     // 팝업 표시 중인 날짜
```
